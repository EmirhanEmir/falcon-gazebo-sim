# FALCON V2 — Gazebo Test Infrastructure

Status: infrastructure/documentation only. **No aircraft physics implementation exists in this repository yet** (no `model.sdf`, no plugin/controller code), so **none of the tests below are implemented or executable** — this file is a registration of planned test scenarios and what each one will check against, for `gazebo-testing` to build out once geometry, aerodynamics, propulsion, and controls implementation exists. Updated 2026-08-21 (post-master-dataset-sync pass) to register 22 additional named scenarios and reconcile them with the prior 19-item list.

**Update, 2026-08-21 (first Gazebo structural implementation pass, `geometry-structure`):** `model/model.sdf` and `model/model.config` now exist for the first time (structural V1: links, joints, mass/CG/inertia, visual/collision geometry — no aerodynamic/propulsion/control plugin). This changes the *executability* of exactly three tests in §1 below — `MODEL_LOAD_TEST`, `GROUND_NO_WIND_TEST`, and `MASS_CG_INERTIA_TEST` can now actually be run against a real file for the first time (a self-check pass by `geometry-structure` already confirms `gz sdf --check` → `Valid.`, correct total mass/CG-consistency/inertia-positive-definiteness/link-graph-connectivity — full detail in `docs/source_of_truth/geometry/GEOMETRY.md` §33.6 — but that self-check is not a substitute for `gazebo-testing` independently running and recording these as formal tests). Every other test below (control-surface sign tests, propulsion tests, aerodynamic/trim/dynamic-mode tests) remains not executable, exactly as before, since no aerodynamic, propulsion, or control-actuation plugin exists yet — this update does not register any new test scenario.

**Update, 2026-08-21 (structural V1 test pass, `gazebo-testing`):** the structural-scope subset of tests is no longer merely planned — it has been implemented and executed against the real `model/model.sdf` in a running Gazebo Sim Harmonic 8.14.0 server (via the `gz-sim` Python `TestFixture` API and, independently, the `gz model`/`gz sdf` CLI tools). 11 tests were run: `MODEL_LOAD_TEST`, `STATIC_GRAVITY_TEST`, `MASS_CG_INERTIA_TEST`, `CONTROL_JOINT_EXISTENCE_TEST`, `CONTROL_JOINT_LIMIT_TEST`, `CONTROL_JOINT_FREE_MOTION_TEST`, `PROP_JOINT_EXISTENCE_TEST`, `PROP_JOINT_AXIS_TEST`, `MESH_PLACEMENT_VISUAL_TEST`, `COLLISION_SANITY_TEST`, `NUMERICAL_STABILITY_IDLE_TEST` — all 11 **PASS**. Full report: `docs/test_results/2026-08-21_structural_v1_test_report.md`. Test worlds: `tests/gazebo/worlds/falcon_v2_freefall_world.sdf`, `tests/gazebo/worlds/falcon_v2_zero_g_world.sdf`. Test scripts and orchestrator: `tests/gazebo/scripts/`. Every other test in this file (control-surface sign tests, propulsion tests, aerodynamic/trim/dynamic-mode tests) remains not executable, exactly as before, since no aerodynamic, propulsion, or control-actuation plugin exists yet.

**Update, 2026-08-22 (structural V1 RE-test pass, `gazebo-testing`, supersedes the 2026-08-21 result):** `validation` raised 2 MAJOR findings against the 2026-08-21 build (hinge secondary/tilt-axis fit not reproducible; unsourced propeller "as viewed from behind" claim). `geometry-structure` corrected both directly in `model/model.sdf` (new deterministic hinge-axis fit changed `<axis><xyz>` and link-origin Z for the 5 control-surface joints by sub-mm to ~1.1 mm; the propeller fix was comment-only, axis/type unaffected). The full 11-test suite was re-run against the corrected file — **all 11 still PASS**, with `MASS_CG_INERTIA_TEST` independently re-confirming mass/CG/inertia were unaffected by the fix (aggregate CG identical to 6 decimal places via a fresh `gz sdf --inertial-stats` run plus an independent runtime ECM query). Full report: `docs/test_results/2026-08-22_structural_v1_retest_hinge_fix.md`. Only one script needed a targeted update, `tests/gazebo/scripts/test_mesh_placement.py` (its hardcoded visual-pose cross-check table now reflects the corrected hinge origins) — every other script queries joint/link state dynamically and required no change. Prior raw evidence archived at `tests/gazebo/results/archive_2026-08-21_pre_hinge_fix/` rather than silently overwritten.

**Update, 2026-08-22 (`AERODYNAMICS_V1_IMPLEMENTATION` pass, `aerodynamics`):** an aerodynamic force/moment System plugin now exists (`plugins/aerodynamics/`, built target `FalconV2Aerodynamics`, attached to `model/model.sdf` via an additive `<plugin>` block — no structural element modified) and is loadable in a live Gazebo instance (`export GZ_SIM_SYSTEM_PLUGIN_PATH=.../plugins/aerodynamics/build:$GZ_SIM_SYSTEM_PLUGIN_PATH` before launching `gz sim`; see `plugins/aerodynamics/README.md`). This changes the executability of §4/§5/§6 below (`ZERO_AIRSPEED_AERO_TEST`, `AOA_SIGN_TEST`, `SIDESLIP_SIGN_TEST`, and the sign-behavior tests) for the first time — they can now be run against real ECM/link/joint state, not just documentation. `aerodynamics` performed (a) a standalone, Gazebo-independent self-test of the pure-math core (`plugins/aerodynamics/build/aero_model_selftest`, 15/16 checks PASS, 1 honestly-reported FAIL) and (b) a live-Gazebo smoke test (plugin loads, configures, runs 3000 physics steps against `tests/gazebo/worlds/falcon_v2_freefall_world.sdf` with no crash/NaN, diagnostics topic confirmed publishing physically sane values) — **neither is a substitute for `gazebo-testing` independently building, running, and recording the formal test suite below.** Full derivation/self-test log: `docs/source_of_truth/aerodynamics/AERODYNAMICS.md` §19.

**Priority flag for `gazebo-testing` (superseded by the 2026-08-22 fix-pass update below — kept as the historical record of the original ask):** `Cma_RESTORING_SIGN_TEST` should be run **first** among the sign tests below. `aerodynamics` found, via a rigorous first-principles FLU pitch-axis analysis (confirmed by both self-tests above), that the literal task-specified `My=qbar·S·c_ref·Cm` formula produces **destabilizing** rather than restoring pitch behavior — a specific, actionable, already-diagnosed finding (`AERODYNAMICS.md` §19.7), not a generic unresolved-sign placeholder. Do not tune/adjust `Cma` or any coefficient to make this pass (`CLAUDE.md` simulation tuning policy) — if confirmed, the fix belongs in the `Cm`-to-`My` axis mapping, and is `aerodynamics`'/`validation`'s decision, not `gazebo-testing`'s.

Diagnostics topic for live inspection while testing: `/model/falcon_v2/aerodynamics/diagnostics` (`gz.msgs.Double_V`, field order `V, alpha_rad, beta_rad, qbar, CL, CD, CY, Cl, Cm, Cn`, published at 20 Hz by default, configurable via the `<diagnostics_rate_hz>` SDF plugin parameter). A wind-input hook exists (`/model/falcon_v2/wind`, `gz.msgs.Vector3d`, world frame) for future use — no publisher exists yet, so wind is always zero in the current build.

**Update, 2026-08-22 (`AERODYNAMICS_V1` live-Gazebo test pass, `gazebo-testing`):** all 16 mandatory aero tests from §4 below (`ZERO_AIRSPEED_AERO_TEST`, `AOA_SIGN_TEST`, `SIDESLIP_SIGN_TEST`, plus `LIFT_SIGN_TEST`/`DRAG_SIGN_TEST`/`Cma_RESTORING_SIGN_TEST`/`Cmq`/`Clp`/`Cnr_DAMPING_SIGN_TEST`/`Cnb_STATIC_STABILITY_SIGN_TEST`/`AILERON_ROLL_SIGN_TEST`/`RUDDER_YAW_SIGN_TEST`/`ELEVATOR_PITCH_SIGN_TEST`/`RATE_NORMALIZATION_TEST`/`DRAG_POLAR_TEST`/`HIGH_ALPHA_LIMITER_TEST`, not previously itemized as separate rows in this file's planned-test tables but named explicitly in this pass's task) were implemented and run against the real `FalconV2Aerodynamics` plugin in a live `gz-sim8` server. **Result: 15/16 PASS, 1 `TEST_FAILED` — `Cma_RESTORING_SIGN_TEST` independently confirms, via real applied-wrench measurement (not just `aerodynamics`' own pure-math self-test), that the pitch axis is destabilizing rather than restoring** — see `docs/test_results/2026-08-22_aerodynamics_v1_test_report.md` for the full writeup, exact numeric evidence, and the two test-methodology findings made along the way (Cmd-based velocity override is unusable for release-and-measure testing in this environment; holding a sustained body rate excites the undamped control-surface/prop joints and must be worked around). Test scripts: `tests/gazebo/scripts/{aero_lib.py, test_aero_kinematic_signs.py, test_aero_stability_derivatives.py, test_aero_control_surfaces.py, run_all_aero_tests.sh}`. Reuses the existing `falcon_v2_zero_g_world.sdf` unmodified (no new world file needed — the plugin is already embedded in `model/model.sdf`). `TRIM_TEST`/`STRAIGHT_LEVEL_FLIGHT_TEST`/dynamic-mode tests remain out of scope for this pass and blocked on the `Cma` fix.

**Update, 2026-08-22 (same day, `AERODYNAMICS_V1` Cm-to-My fix re-test, `gazebo-testing`):** `aerodynamics` applied a scoped Cm-to-My sign correction (negate only the static `Cm0+Cma*alpha+Cmde*deltaE` group when computing applied `My`, leaving the rate group `Cmq*qHat` untouched) plus a previously-missing `CLq*qHat` term in `CL`, both code-only (no `aero_v1_config.yaml` coefficient changed). The full 16-test suite was re-run against the rebuilt plugin, reusing the same scripts/methodology (only the independent Python replica in `aero_lib.py` and the `HIGH_ALPHA_LIMITER_TEST` cross-check were updated to mirror the new formula). **Result: 16/16 PASS — `Cma_RESTORING_SIGN_TEST` now confirms a restoring pitch moment live, closing out the prior `TEST_FAILED`.** `ELEVATOR_PITCH_SIGN_TEST`'s measured sign flipped as `aerodynamics` anticipated (documented, not a regression); every other test is numerically unchanged from the first run, confirming the fix's scope was as claimed. Full writeup: `docs/test_results/2026-08-22_aerodynamics_v1_retest_cma_fix.md` (supersedes `docs/test_results/2026-08-22_aerodynamics_v1_test_report.md`, which remains as the historical record of the original finding and methodology development). Ready for `validation`'s final review.

**Update, 2026-08-22 (`AERODYNAMICS_V1_IMPLEMENTATION` fix sub-pass, `aerodynamics`):** following `gazebo-testing`'s live confirmation above and `validation`'s root-cause review, two fixes were applied to `plugins/aerodynamics/AeroModel.hh`'s `ComputeAero()` (no `aero_v1_config.yaml` coefficient value changed): (1) a precisely-scoped `Cm`-to-`My` sign correction — only the static/angle-derived group (`Cm0 + Cma·alpha + Cmde·delta_e`) is negated when computing `My`; the self-referential rate group (`Cmq·q_hat`) is left untouched (flipping it would have broken the already-correct `Cmq_DAMPING_SIGN_TEST`); (2) the previously-loaded-but-unused `CLq·q_hat` term is now included in the `CL` build-up (MAJOR finding). Full derivation: `AERODYNAMICS.md` §19.12. Plugin rebuilt clean; standalone self-test now **17 PASS, 0 FAIL** (up from 15/1); live-Gazebo smoke-load re-verified (no crash/NaN). **Consequence to expect, not a regression:** `Cmde` moved into the negated group, so `ELEVATOR_PITCH_SIGN_TEST`'s measured `My` sign will flip relative to the 2026-08-22 test-report result above. **Requested from `gazebo-testing`:** re-run at minimum `Cma_RESTORING_SIGN_TEST`, `Cmq_DAMPING_SIGN_TEST` (regression check), `ELEVATOR_PITCH_SIGN_TEST` (expected flip), and ideally `ZERO_AIRSPEED_AERO_TEST`/`DRAG_POLAR_TEST`/`HIGH_ALPHA_LIMITER_TEST` (since `CL` changed) against the rebuilt plugin. `validation` re-review follows before `AERODYNAMICS_V1_READY`.

**Update, 2026-08-22 (`CONTROL_SURFACE_SIGN_MAPPING` test pass, `gazebo-testing`):** independently verified `controls-integration`'s geometric TE-direction/aero-moment-sign determination for the 5 control-surface joints via 9 live-Gazebo tests: `LEFT_AILERON_JOINT_SIGN_TEST`, `RIGHT_AILERON_JOINT_SIGN_TEST`, `AILERON_DIFFERENTIAL_SIGN_TEST`, `AILERON_COMMON_MODE_REJECTION_TEST`, `LEFT_ELEVATOR_JOINT_SIGN_TEST`, `RIGHT_ELEVATOR_JOINT_SIGN_TEST`, `RUDDER_JOINT_SIGN_TEST`, `AILERON_AERO_MOMENT_SIGN_TEST`, `RUDDER_AERO_MOMENT_SIGN_TEST`. **Result: 9/9 PASS, 9/9 CONFIRMS-HYPOTHESIS** — both aileron joints and both elevator joints independently, quantitatively confirmed same-sign (not mirrored, TE-up for +angle on both sides in each pair); rudder confirmed TE-right(−Y) for +angle; `aileron_sign=+1` and `rudder_sign=+1` (unmodified `aero_v1_config.yaml`) both independently confirmed correct via real measured applied moment (Mx=+4.87685 N·m for the differential aileron case, exactly Mx=0 for the common-mode case, Mz=−0.44639 N·m for the rudder case). No REFUTES-HYPOTHESIS result. `ELEVATOR_SYMMETRIC_MAPPING_TEST`/`ELEVATOR_AERO_MOMENT_SIGN_TEST` explicitly deferred, pending `elevator_sign` config fix (`controls-integration`'s coordination item with `aerodynamics`, not executed this pass). Full report: `docs/test_results/2026-08-22_control_surface_sign_mapping_test_report.md`. New script: `tests/gazebo/scripts/test_control_surface_sign_mapping.py` (reuses `aero_lib.py`'s existing helpers, no new technique). No aircraft physics parameter modified.

**Update, 2026-08-22 (same day, `CONTROL_SURFACE_SIGN_MAPPING` follow-up, `gazebo-testing`):** after `aerodynamics` applied the `elevator_sign` fix (`+1.0 -> -1.0` in `aero_v1_config.yaml`), ran the 2 deferred tests: `ELEVATOR_SYMMETRIC_MAPPING_TEST` and `ELEVATOR_AERO_MOMENT_SIGN_TEST` — **both PASS/CONFIRMS-HYPOTHESIS**. Same physical TE-up command (`left_elevator_joint=right_elevator_joint=+8°`) now measures My=-1.48560 N·m (nose-up), flipped from the pre-fix +1.128 N·m (nose-down) exactly as anticipated, and matching the textbook TE-up⇒nose-up expectation for the first time. Optional regression re-check (`test_aero_stability_derivatives.py`, unmodified) confirms `Cma_RESTORING_SIGN_TEST`/`Cmq_DAMPING_SIGN_TEST`/others still PASS, unaffected by the elevator-only config change. **Combined result across both `CONTROL_SURFACE_SIGN_MAPPING` passes: 11/11 CONFIRMS-HYPOTHESIS, 0 REFUTES-HYPOTHESIS.** Full writeup appended to `docs/test_results/2026-08-22_control_surface_sign_mapping_test_report.md` (see "Supplement" section). New script: `tests/gazebo/scripts/test_elevator_sign_mapping.py`. No aircraft physics parameter modified. Ready for `validation`'s final review.

**Update, 2026-08-23 (`TRIM_AND_DYNAMIC_AERO_VALIDATION`, `gazebo-testing`):** live-Gazebo reproduction of `aerodynamics`' desk-computed neutral trim point (Benchmark A, V=21.244 m/s) and the 18 m/s/elevator=-8° trim point (Benchmark B), plus a magnitude/direction sanity check of the 5 dynamic-derivative runtime responses (Cmq/Clp/Cnr damping, Cma restoring, Cnb directional stability). **Result: Benchmark A PASS, Benchmark B PASS vs the V1 model's own desk-computed values (CL/CD/Cm/My all matched to ~1e-6–1e-7), Benchmark B vs the XFLR5/6kg-level-flight reference is KNOWN_V1_LIMITATION (not PASS/FAIL, ~+3.2% CL/+3.6% drag — already root-caused by `aerodynamics` as the expected consequence of the V1 model's omitted `CL_delta_e` term), all 5 dynamic-derivative checks PASS (correct sign, live-measured magnitude within a factor of ~1.4 of the task's quoted example once scaled to a common V/rate condition).** New controller technique (`test_trim_benchmarks.py`, not added to `aero_lib.py`): feedforward + light-P holding, needed because `aero_lib.hold_step()`'s pure-P control has a non-decaying steady-state error against a persistent ~59 N aero force (empirically ~0.06 m/s residual at the standard kp_lin=150, ~300x too large for this task's ±1e-4 CL tolerance) — the feedforward term is computed once from `aero_lib.compute_aero()` at the exact target state and applied through the same `add_world_force`/`add_world_wrench` primitives, so the quantity actually measured (the live plugin's own output) is unaffected. New scripts: `tests/gazebo/scripts/test_trim_benchmarks.py`, `tests/gazebo/scripts/test_dynamic_derivative_response.py` (the latter cites `aero_stability_derivatives_result.json`'s existing live measurements rather than relaunching Gazebo, per task instruction, since that file already had quantitative moment values for all 5 checks). No aircraft physics parameter modified. Full report: `docs/test_results/2026-08-23_trim_and_dynamic_aero_validation_report.md`.

**Update, 2026-08-23 (`PROPULSION_V1_IMPLEMENTATION`, `gazebo-testing`):** live-Gazebo verification of the new twin-motor propulsion System plugin (`plugins/propulsion/`, `FalconV2Propulsion`), additively attached alongside `plugins/aerodynamics/` on `model/model.sdf`. `propulsion`'s standalone pure-math self-test rebuilt fresh and reconfirmed 11/11 PASS before any live testing. **PRIORITY CHECK result: `NO_DUPLICATION`** — the specific, self-flagged risk that `Joint::SetVelocity()`'s internal constraint torque might double-count the explicit `AddWorldWrench` reaction torque was directly measured (steady-state single-motor roll-release technique) and found to match the single-source analytical prediction to ~1% (ratio 0.9895), not ~2×. **All 16 named tests: PASS** (`MOTOR_ZERO_THROTTLE_TEST`, `MOTOR_SPINUP_TEST`, `MOTOR_SPINDOWN_TEST`, `RPM_STATE_CONTINUITY_TEST`, `LEFT_PROP_VISUAL_OMEGA_TEST`, `RIGHT_PROP_VISUAL_OMEGA_TEST`, `RPM_SAFETY_LIMIT_TEST`, `STATIC_THRUST_RUNTIME_TEST`, `CURRENT_POWER_RUNTIME_TEST`, `PROP_REACTION_TORQUE_SIGN_TEST`, `COUNTER_ROTATION_CANCELLATION_TEST`, `HUB_FORCE_APPLICATION_TEST`, `DIFFERENTIAL_THRUST_MOMENT_TEST`, `LEFT_ENGINE_OUT_TEST`, `RIGHT_ENGINE_OUT_TEST`, `ZERO_RPM_NUMERICAL_SAFETY_TEST`). Static RPM sweep vs the raw APC table matched to <0.1% at all 4 tabulated points (5000/6000/9000/10000 RPM); the SunnySky static-bench comparison (throttle=1.0) showed +13.2% RPM/+24.7% thrust/+2.8% current/+2.9% power gaps, root-caused as largely explained by the naturally-higher settling RPM (RPM²-scaling predicts ~+28% thrust from the RPM gap alone) plus the already-documented ~4.7–5% APC-vs-bench discrepancy, not a new defect; the 18 m/s cruise natural operating point (throttle chosen independently, never solved-for) landed within +0.8% RPM/+10.0% thrust of the `V1_VALIDATION_ESTIMATES` reference at throttle=0.5. Also investigated (real `gz sim -s -r` server, not `TestFixture`) `propulsion`'s reported "one-shot CLI throttle publish didn't take effect on LEFT" observation: confirmed as a topic-discovery-timing artifact (gz-transport topics are not latched; republish-after-discovery always recovers a dropped one-shot; an order-controlled interleaved LEFT/RIGHT trial batch showed 5/5 symmetric success on both sides) rather than a reproducible left-specific code defect — code inspection confirms `OnThrottleLeft`/`OnThrottleRight` are symmetric. No genuine implementation bug found; no propulsion coefficient/mass/CG/inertia/hub-position/APC-value/motor-parameter was changed. New scripts: `tests/gazebo/scripts/{propulsion_lib.py, test_propulsion_priority_check.py, test_propulsion_electrical_dynamics.py, test_propulsion_reaction_torque.py, test_propulsion_operating_points.py}`. Full report: `docs/test_results/2026-08-23_propulsion_v1_test_report.md`. Ready for `validation`'s independent review.

**Update, 2026-08-23 (`PROPULSION_V1_IMPLEMENTATION` targeted re-test, `gazebo-testing`, post `propulsion` SetVelocity→ResetPosition fix):** `validation` found a MAJOR gap — the original priority check above measured the `SetVelocity()`-vs-`AddWorldWrench()` duplication risk only at settled equilibrium (`dω/dt≈0`), a condition under which the hypothesized extra term is zero by construction; `propulsion` rebuilt `PropulsionSystem.cc`/`.hh` to drive `left_prop_joint`/`right_prop_joint` via `Joint::ResetPosition()` (direct kinematic write) instead of `Joint::SetVelocity()` (force-based servo). This targeted pass re-verified only what the fix touches, not the full 16-test suite. Rebuild-confirmed (`.o`/`.so` mtimes newer than source; self-test still 11/11 PASS). **NEW `TRANSIENT_REACTION_TORQUE_ISOLATION_TEST` (the key result): `NO_DUPLICATION_TRANSIENT_CONFIRMED`** — commanded a throttle step (left 0→0.6) and released only roll DURING active spin-up (not after settling; domega/dt up to ~2582 rad/s² observed, vs ~0 at the prior check), using a tick-resolution theta-derived (`Joint.position()`, not `Joint.velocity()`) reconstruction of omega/Q_prop as the "at that same instant" ground truth. A least-squares-through-origin fit across 20 sub-windows spanning the whole transient gave slope=0.9862, r=1.0000; the same-instant smoking-gun check at the sub-window with the largest domega/dt (corrected 2026-08-23, round 2 fix-loop below — originally mis-reported as "0.37%" from two different sub-windows; the true same-instant ratio is 0.0958%) found the residual only 0.0958% of that hypothesized-duplication scale. **This script's own `LEFT_PROP_VISUAL_OMEGA_TEST_REGRESSION`/`RIGHT_PROP_VISUAL_OMEGA_TEST_REGRESSION` sub-checks both PASS** under the new mechanism — `Joint.velocity()` no longer tracks omega at all post-fix (confirmed: stays <0.2 rad/s while the joint spins at >700 rad/s), an expected consequence of `ResetPosition()` having no velocity-component side effect, not a bug; these sub-checks reconstruct omega from tick-to-tick `Joint.position()` differencing and confirm it still matches an independent synchronous pure-Python ODE replica (max err 2.59 rad/s / 0.35% of peak). **Correction (round 2 fix-loop, see 2026-08-23 update below):** this paragraph originally implied the like-named `LEFT_PROP_VISUAL_OMEGA_TEST`/`RIGHT_PROP_VISUAL_OMEGA_TEST` keys in `propulsion_electrical_dynamics_result.json` (produced by `test_propulsion_electrical_dynamics.py`, a *different* script) were "measured correctly via theta-reconstruction" too — false as written: that script was NOT re-run this pass and still read `Joint.velocity()` directly, so its saved results at the time were a stale carry-over from before the SetVelocity→ResetPosition fix (still numerically valid for the underlying rotor ODE, since the fix didn't touch that math, but methodologically invalid post-fix until the round-2 correction below re-ran it against theta-differencing). **Regression: `PROP_REACTION_TORQUE_SIGN_TEST`/`COUNTER_ROTATION_CANCELLATION_TEST` both still PASS** at steady state (0.97% magnitude error and exact machine-zero cancellation respectively, both consistent with the original pass's numbers) — `HUB_FORCE_APPLICATION_TEST`/`DIFFERENTIAL_THRUST_MOMENT_TEST`/engine-out tests intentionally NOT re-run (unaffected by this fix, out of scope for this pass). **Test-harness bug fixed:** `propulsion_electrical_dynamics_result.json`'s `RPM_STATE_CONTINUITY_TEST.detail.continuity_bound_rad_s` was 10× too large (25.86 rad/s instead of the correct 2.586 rad/s) — an arithmetic/naming conflation in `test_propulsion_electrical_dynamics.py` (a 10x pass/fail safety margin and the underlying theoretical bound were computed under one variable name; the margined value was the one exposed), not a units bug; fixed in the script (now exposes `continuity_bound_rad_s`, `continuity_margin_multiplier`, `continuity_pass_threshold_rad_s` as distinct fields) and hand-corrected in the existing result JSON (a pure config-derived constant, no rerun needed). New scripts: `tests/gazebo/scripts/{test_propulsion_transient_reaction_torque.py, test_propulsion_reaction_torque_regression_10_11.py}`. New/updated results: `tests/gazebo/results/{propulsion_transient_reaction_torque_result.json, propulsion_reaction_torque_result.json (updated in place), propulsion_electrical_dynamics_result.json (continuity-bound field corrected)}`. No aircraft physics parameter modified. **`propulsion`'s SetVelocity→ResetPosition fix is confirmed sufficient** — the specific transient-only gap `validation` identified is closed, with direct quantitative evidence, not just steady-state re-confirmation. Full re-test section appended to `docs/test_results/2026-08-23_propulsion_v1_test_report.md`. Ready for `validation`'s independent review.

**Update, 2026-08-23 (`PROPULSION_V1_IMPLEMENTATION` fix-loop round 2, `gazebo-testing`, contradiction reconciliation):** `validation` flagged a genuine contradiction between `test_propulsion_transient_reaction_torque.py` (round 1, correctly found `Joint.velocity()` no longer tracks omega post `ResetPosition` fix, ~0.17 rad/s stale vs ~730 rad/s real) and `test_propulsion_electrical_dynamics.py` (whose saved `LEFT_PROP_VISUAL_OMEGA_TEST`/`RIGHT_PROP_VISUAL_OMEGA_TEST`/etc. results, still `Joint.velocity()`-based, reported good tracking). **Root cause, established via a side-by-side same-tick diagnostic (reads both `Joint.velocity()` and `Joint.position()`-differenced omega on identical ticks of the identical run) plus directly re-invoking `test_propulsion_electrical_dynamics.py`'s own functions against the current `.so`:** `Joint.velocity()` genuinely does NOT track omega post-fix — confirmed directly, reproducibly, 3 independent ways. `test_propulsion_electrical_dynamics.py`'s saved results were a stale carry-over from ITS ORIGINAL run (2026-08-23, before `propulsion`'s SetVelocity→ResetPosition fix, when `Joint.velocity()` genuinely was valid — see §1/§2 of `docs/test_results/2026-08-23_propulsion_v1_test_report.md`, captured under the old mechanism); the script was never re-run after the fix, and later hand-edits to its result JSON (the `continuity_bound_rad_s` field-naming fix) updated the file's mtime without a live re-run, making the stale results look current. **Fix:** `test_propulsion_electrical_dynamics.py` rewritten to reconstruct omega from `Joint.position()` (theta), finite-differenced tick-to-tick — the same technique `test_propulsion_transient_reaction_torque.py` already used — for ALL 6 affected named tests (`MOTOR_ZERO_THROTTLE_TEST`, `MOTOR_SPINUP_TEST`, `MOTOR_SPINDOWN_TEST`, `RPM_STATE_CONTINUITY_TEST`, `LEFT_PROP_VISUAL_OMEGA_TEST`, `RIGHT_PROP_VISUAL_OMEGA_TEST`), and re-run. **Result: all 6 still PASS**, with numbers numerically consistent with the original (pre-fix) run to high precision (e.g. `LEFT_PROP_VISUAL_OMEGA_TEST` max_err=2.5863 rad/s, spin-up 6978.69 RPM equilibrium unchanged) — confirming the underlying rotor ODE is genuinely unaffected by the SetVelocity→ResetPosition fix (as intended, a purely cosmetic Gazebo-glue change), now measured via a methodologically valid channel. Also fixed (MINOR, `validation`): `propulsion_transient_reaction_torque_result.json`'s `smoking_gun_residual_ratio` was computed from `max_early_residual_Nm`/`max_early_hypothesized_extra_torque_Nm` at two DIFFERENT sub-windows (t=0.135s / t=0.015s) despite being described as a same-instant ratio; recomputed as a genuine same-instant ratio at the single sub-window with the largest `|domega/dt|` (t=0.015s): **0.0958%** (was mis-reported as 0.37%) — new fields `peak_domega_dt_t_center_s`/`peak_domega_dt_residual_Nm`/`peak_domega_dt_hypothesized_extra_torque_Nm` replace the old two-different-window fields; verdict (`NO_DUPLICATION_TRANSIENT_CONFIRMED`) unaffected. No aircraft physics parameter modified; `plugins/propulsion/` untouched (test-methodology/reporting fix only). Updated files: `tests/gazebo/scripts/test_propulsion_electrical_dynamics.py`, `tests/gazebo/scripts/test_propulsion_transient_reaction_torque.py`, `tests/gazebo/results/propulsion_electrical_dynamics_{log.txt,result.json}`, `tests/gazebo/results/propulsion_transient_reaction_torque_{log.txt,result.json}`, `docs/test_results/2026-08-23_propulsion_v1_test_report.md`. Ready for `validation`'s independent review.

**Update, 2026-08-23 (`POWERED_TRIM_AND_FREE_FLIGHT_VALIDATION`, `gazebo-testing`):** searched for the real powered straight-and-level trim point near V=18 m/s using the existing aerodynamics+propulsion plugins/control mapping (control inputs only - throttle, elevator - no aircraft physics parameter touched), then confirmed the selection in a genuinely free 6-DOF run. 4-round search (coarse grid -> refine 1 -> refine 2 -> settled/tail-window recheck -> directional extrapolation, 33 total candidate evaluations, `any_nan=False` throughout) found **throttle=0.4915, elevator=+5.5deg physical (delta_e_aero=-5.5deg)** settles to a near-zero tail-window vertical speed (+0.019 m/s over a 12s check). A 30s (29s observed) fully-free Phase 2 run (no velocity/attitude/kinematic hold of any kind after release) confirmed this: airspeed 18.165->17.838 m/s, altitude 99.39->101.16m (+1.77m drift), vertical speed converging to +0.02 to +0.09 m/s in the settled tail, pitch settling to a mild -2.4 to -2.7deg oscillation (peak -4.11deg at t~4s), roll/yaw exactly 0.00deg throughout, no NaN/Inf. **PASS against explicitly pre-stated tolerances**, not a mathematically perfect zero-drift trim (small residual climb, consistent with the already-documented +4% Lift-over-prediction `KNOWN_V1_LIMITATION` from the missing `CL_delta_e` term). Two test-methodology findings made and resolved within this pass (not aircraft-physics issues): (1) holding body-frame linear velocity while leaving angular rate fully free is unsafe for this airframe's harness beyond a few tens of ms (produces non-physical runaway rotation); (2) a short 2s post-release window's mean vertical speed is not a reliable settled-trim proxy for this airframe - a longer tail-window check is required. New scripts: `tests/gazebo/scripts/{test_powered_trim_search.py, test_powered_free_flight_validation.py}`. Full report: `docs/test_results/2026-08-23_powered_trim_and_free_flight_validation_report.md`. No aircraft physics parameter modified. Ready for `validation`'s independent review.

**Update, 2026-08-23 (`FREE_FLIGHT_DYNAMIC_RESPONSE_VALIDATION`, `gazebo-testing`):** from the
already-validated powered trim point (`POWERED_TRIM_AND_FREE_FLIGHT_VALIDATION`, reused directly, not
re-searched), applied 5 small-signal initial perturbations (one per classical mode — short-period,
phugoid, roll subsidence, Dutch roll, spiral) and observed the aircraft's NATURAL free 6-DOF response
with `base_link` completely unheld and the 5 control-surface joints pinned at trim/neutral (not a
flight-controller correction — no actuator/servo model exists yet). **Result: 5/5 PASS** — short-period
fast/damped (fd=1.95Hz vs ref 2.11Hz, zeta=0.56 vs ref 0.39); phugoid slow/bounded/oscillatory
(period=8.6s vs ref 10.2s, more damped than XFLR5's near-neutral reference, flagged INFO not a defect);
roll rate strongly damped (tau=0.112s vs ref 0.106s, excellent match); Dutch roll coupled/damped
(fd=0.42Hz vs ref 0.51Hz); spiral a mild slow LATERAL TREND with time-constant magnitude comparable to
the reference but opposite sign (convergent vs XFLR5's mildly divergent prediction — investigated,
flagged INFO for `aerodynamics`/`validation`, not asserted as a bug, per the task's own
weakly-stable-or-unstable acceptance bar). `any_nan=False` all 5 tests; propulsion stayed left-right
symmetric to machine precision for the 2 purely-longitudinal tests and to <0.06% for the 3 lateral
tests (expected asymmetric-hub-velocity physics, not an anomaly). A methodology finding was made and
fixed within this pass (not a physics issue): the first same-sign-peak/single-baseline damping-ratio
method produced a spurious negative (growing) result for test A, traced to only 2 available peaks
contaminated by slower phugoid-mode drift within the short observation window; fixed via an
adjacent-extrema swing-based, data-driven-monotonic-prefix method (`merged_oscillation_metrics()`) and
a pre-zero-crossing fit-window restriction for test C's roll-rate exponential fit
(`truncate_before_sign_change()`). No aircraft physics parameter modified. New script:
`tests/gazebo/scripts/test_free_flight_dynamic_response.py` (reuses `aero_lib.hold_step()`/
`propulsion_lib.py` unchanged). Full report:
`docs/test_results/2026-08-23_free_flight_dynamic_response_validation_report.md`. Ready for
`validation`'s independent review.

**Update, 2026-08-24 (`ACTUATOR_SERVO_MODEL_V1`, `gazebo-testing`):** verified `controls-integration`'s new real torque/effort-based actuator/servo plugin (`plugins/actuators/`, `FalconV2Actuators`) which now drives the 5 control-surface joints via `Joint::SetForce()` (PD control law toward a rate-limited/clamped setpoint, `Joint::SetVelocityLimits()`/`SetEffortLimits()` as physics-engine hard backstops), replacing this suite's own prior `reset_position()`-based joint injection. Standalone self-test re-confirmed 10/10 PASS. **4 required mapping/integration tests (zero-g world, isolates the mapping question from gravity): 4/4 PASS** — `ACTUAL_JOINT_STATE_FEEDS_AERO_TEST` (measured real applied `My` at 15ms/9% of target vs 350ms/97.5% of target since the command step, confirming the aero plugin tracks the ACTUAL servo-tracked position, not an instant jump), `LEFT_RIGHT_ELEVATOR_MAPPING_REGRESSION`, `LEFT_RIGHT_AILERON_MAPPING_REGRESSION`, `RUDDER_MAPPING_REGRESSION` (all settle within ~0.03–0.14° of target and reproduce the already-verified sign mapping). **Small-deflection flight-load test (±5°/±10°, gravity ON, powered trim condition V=18.165 m/s/throttle=0.4915 reused read-only from `POWERED_TRIM_AND_FREE_FLIGHT_VALIDATION`), 12 cases: TEST_FAILED, 8/12** — all 4 `ELEVATOR_SYMMETRIC_*` and all 4 `AILERON_DIFFERENTIAL_*` cases settle into a stable, non-oscillatory, non-effort-saturated equilibrium 2.6–4.2° off the commanded target (up to 84% relative error); all 4 `RUDDER_*` cases PASS cleanly (≤0.012° residual). **Root cause confirmed analytically and numerically**: gravity acting on the control-surface links' own `TEMPORARY_NUMERICAL_MASS` placeholder weight creates a real, constant disturbance torque about any hinge axis with a horizontal (pitch-axis-aligned) component — elevator/aileron hinges are Y-dominant (droop), rudder's is Z-dominant (zero gravity moment about a vertical axis from a vertical force, by construction — matches its clean result exactly); a pure-PD (no integral term) control law settles at a nonzero `droop = T_gravity/kp` for any constant disturbance regardless of `kp` size, and `actuator_v1_config.yaml`'s own `kp` was derived only from closed-loop bandwidth vs. the placeholder inertia, never against a disturbance-rejection criterion. First-principles `T=x_local·m·g`, `droop=T/kp` prediction (elevator −3.27°, aileron −4.19°, rudder exactly 0°) matches observed droop to ~5–9% for all three surfaces — decisive, not coincidental. Isolated single-joint diagnostic confirms: 0° droop in zero-g, small residual in gravity-on/base-free (equivalence-principle near-cancellation), full droop in gravity-on/base-held (matching both this test's `hold_step` mechanism AND real sustained/trimmed flight, which is likewise not free-falling) — so this is a genuine actuator-control-law finding relevant to real flight, not merely a test-harness artifact. **±45° mechanical/mesh sanity check (zero-g, avoids the gravity-droop confound): 5/5 PASS**, hinge-pivot invariant holds to ≤1 micrometer at all 3 angles for all 5 surfaces, no `GEOMETRY_REVIEW_REQUIRED` (AABB overlap vs. the nearest fixed reference collision box grows only mildly at ±45° vs. the 0° baseline, well under the 5× dramatic-growth flag threshold used) — scope note: `model.sdf`'s model-level `self_collide=false` means no live Gazebo contact-event channel exists for these surfaces regardless of angle, so this check is geometry-only. No NaN/Inf, no chatter/oscillation anywhere in this pass — every failure above is a clean, stable, but WRONG steady state, not an instability. No aircraft physics parameter modified by `gazebo-testing`. New scripts: `tests/gazebo/scripts/{actuator_lib.py, test_actuator_aero_integration.py, test_actuator_flight_load.py, test_actuator_mechanical_sanity.py}`. Full report: `docs/test_results/2026-08-24_actuator_servo_model_v1_test_report.md`. **Stage verdict: NOT_READY** — needs a `controls-integration` fix-loop (integral/feedforward disturbance rejection in the control law) before `validation` review; `gazebo-testing` did not attempt this fix itself per the hard constraint against tuning control/physics parameters to pass a test.

**Update, 2026-08-24 (`ACTUATOR_SERVO_MODEL_V1_GRAVITY_DROOP_FIX` re-test, `gazebo-testing`):** targeted re-test only, per explicit instruction — re-ran `tests/gazebo/scripts/test_actuator_flight_load.py` (the 12-case gravity-on flight-load test above) against `controls-integration`'s PID (integral-action) fix to `plugins/actuators/ActuatorModel.hh`/`ActuatorSystem.{cc,hh}` (`ki_nm_per_rad_s=6.9576e-03`, conditional-integration anti-windup, pole-placement-derived, three real negative closed-loop roots, Routh–Hurwitz margin ~100×); `test_actuator_aero_integration.py`/`test_actuator_mechanical_sanity.py` deliberately NOT re-run (confirmed unaffected by mtime — `plugins/aerodynamics/` and all mesh/geometry untouched). Build confirmed current (`libFalconV2Actuators.so` mtime postdates all 3 source files; `cmake --build .` produced no new compile output); standalone self-test 12/12 PASS (up from 10/10, +`ACTUATOR_INTEGRAL_ANTI_WINDUP_TEST`/`ACTUATOR_INTEGRAL_DISTURBANCE_REJECTION_INFO` reproducing the bug fixed: `ki=0` residual=−3.294°, `ki=6.9576e-03` residual=−0.0000°). **The original steady-state gravity-droop bug is CONFIRMED FIXED: all 12 cases now settle to 0.0000°–0.0905° residual** (sub-0.1°, matching rudder's pre-existing precision), `settle_ok=True`/`sign_ok=True`/`any_nan=False` in all 12, no spurious `target_clamp_active`/`effort_clamp_active`, tail-window noise ≤0.0096° and tail `|rate|` ≤0.00035 rad/s (no new oscillation/chatter). Two test-infrastructure-only changes made for this re-test (no physics/control-law parameter touched): (1) `SETTLE_STEPS`/`TAIL_STEPS` widened 0.6s/0.15s → 4.5s/0.5s tail window, since the new integral term's ~2.09 rad/s dominant pole (τ≈0.48s) needs several seconds to fully settle, and the original window (calibrated for the pure-PD law's fast ~100 rad/s response) was far too short; (2) a **pre-existing `sign_ok` test-script bug found and fixed**: the check compared the aero-convention `delta_e_aero` against the raw commanded physical joint sign without applying `elevator_sign=-1.0` (`VERIFIED_BY_GAZEBO_GEOMETRY_SIGN_TEST` — a real, correct, unrelated convention), making `sign_ok` spuriously `False` on every `ELEVATOR_SYMMETRIC_*` case independent of actuator behavior (masked pre-fix only because `settle_ok` already failed there for the droop reason; invisible for aileron/rudder since their sign constants are `+1.0`) — fixed to match `test_actuator_aero_integration.py`'s own already-correct `expected_deg = CFG.elevatorSign * ele_deg` convention. **New open finding (not resolved here): 6/12 cases (all 4 `AILERON_DIFFERENTIAL_*`, `ELEVATOR_SYMMETRIC_-5/-10DEG`) show a transient overshoot of 1.96–3.46° peaking ~50–150ms after the command step**, before settling cleanly and monotonically (no ringing, confirmed by supplementary time-series inspection) to the sub-0.1° residual within the predicted window — driven by the interaction of the integral term's gravity-compensating pre-charge (built up during the test's 0.3s neutral-hold warm-up) with the new setpoint step, a standard PID transient-overshoot phenomenon rather than instability, but a genuinely new characteristic (the pure-PD baseline could never overshoot). Reported for `controls-integration`/`validation` to triage; no `kp`/`kd`/`ki`/anti-windup value touched to investigate or resolve it. Updated in place: `tests/gazebo/scripts/test_actuator_flight_load.py`, `tests/gazebo/results/actuator_flight_load_{log.txt,result.json}`. Full re-test section appended to `docs/test_results/2026-08-24_actuator_servo_model_v1_test_report.md`. **Stage verdict: original blocking defect resolved, but not yet clear for `validation` sign-off** pending triage of the overshoot finding above.

**Update, 2026-08-24 (`ACTUATOR_SERVO_MODEL_V1_OVERSHOOT_FIX` re-test round 2, `gazebo-testing`):** targeted re-test only, per explicit instruction — re-ran the full 12-case `tests/gazebo/scripts/test_actuator_flight_load.py` (all 12, not just the 6 that previously overshot) against `controls-integration`'s round-2 fix: standard PID setpoint weighting on the P-term only (`ActuatorConfig.spWeightB=0.7`, new field, default `1.0` preserves old behavior; `PidEffort()`'s P-term now uses `errorP = spWeightB*setpointRad - actualAngleRad`, I/D terms unchanged) — proven, and reconfirmed by the standalone self-test's `ACTUATOR_SETPOINT_STEP_OVERSHOOT_TEST`, to change only a transfer-function zero, not the closed-loop poles, so round-1's steady-state droop fix is algebraically independent of it. `test_actuator_aero_integration.py`/`test_actuator_mechanical_sanity.py` not re-run (control-law-only change, confirmed unaffected by mtime). Build confirmed current (`cmake --build .`: no recompilation, `.so` mtime postdates all sources); standalone self-test 13/13 PASS (up from 12/12). `SETTLE_STEPS`/`TAIL_STEPS` reused unchanged from the round-1 re-test (4.5s/0.5s) per task instruction. **Round-1's steady-state fix STILL HOLDS, 12/12** — all residuals remain 0.0002°–0.0884° (sub-0.1°), `settle_ok=True`/`sign_ok=True` in every case, no regression. **Transient overshoot substantially reduced but not fully resolved**: of the 6 previously-overshooting cases, 4 (`ELEVATOR_SYMMETRIC_-5/-10DEG`: 1.9757°→0.5357°, 1.9621°→0.0163°; `AILERON_DIFFERENTIAL_±10DEG`: 3.4645°→0.2937° both) are now comfortably under the test's 1° bar (91–99% reduction); but **`AILERON_DIFFERENTIAL_±5DEG` still peak at 1.1054° each (60% reduction from 2.7704°, but still 10.5% over the 1° threshold)** — contradicting `controls-integration`'s own reduced-order self-test prediction of exactly 0.0000° overshoot at `spWeightB=0.7`, confirming the caveat they themselves flagged: the real gravity hinge-moment disturbance is angle-dependent/pendulum-like, not the constant-torque idealization their self-test uses, and the live measurement doesn't fully match it for these 2 of 12 cases. The other 6 cases (elevator commands opposing gravity, all 4 rudder) show no new problem — their already-small overshoot shrank further too (general P-kick reduction, not gravity-specific). No NaN/Inf, no new oscillation/chatter, `target_clamp_active`/`effort_clamp_active` stayed `False` throughout — clean numerically. Settling time (from the newly-saved decimated time-series, see below) is ~0.85–1.1s to a tight ~0.1–0.15° band in the worst cases, well within the 4.5s window — not absurdly slow. **New this round**: a decimated (every 10 ticks = 10ms resolution) raw `theta(t)` time-series is now saved into `actuator_flight_load_result.json` (`raw_timeseries_decimated` field) for the 4 worst round-1 overshoot cases, addressing `validation`'s round-1 gap that the overshoot claim was previously demonstrated only via an ad hoc, unrecorded diagnostic. Updated in place: `tests/gazebo/scripts/test_actuator_flight_load.py`, `tests/gazebo/results/actuator_flight_load_{log.txt,result.json}`. Full round-2 section appended to `docs/test_results/2026-08-24_actuator_servo_model_v1_test_report.md`. **Stage verdict: still NOT fully clear for `validation` sign-off** — round-1's blocking defect remains fixed and this round's fix is a large real improvement, but 2 of 12 cases (`AILERON_DIFFERENTIAL_±5DEG`) still exceed the test's own overshoot bar and need a further `controls-integration` iteration or an explicit `validation`-endorsed acceptance decision.

**Update, 2026-08-26 (`CONTROL_AUTHORITY_EFFECTIVENESS_VALIDATION`, `gazebo-testing`):** RAW MEASUREMENT DATA ONLY pass (no MATCH/GAP classification - that comparison against the new XFLR5 small-signal control derivatives is a separate `aerodynamics` pass) comparing the real Actuator V1 servo model -> real joint -> `AerodynamicsSystem` (reading the ACTUAL joint position every tick) control-authority chain, in the validated +/-2/+/-5/+/-10 deg region only, at the already-established powered trim operating point (throttle=0.4915, V=18.165 m/s, alpha=2.461deg, elevator=+5.50deg physical both sides, reused read-only from `POWERED_TRIM_AND_FREE_FLIGHT_VALIDATION`). **21 quasi-static points (7 elevator/7 aileron/7 rudder), all 5 control surfaces commanded every tick via the REAL actuator topics (no `ResetPosition`/teleport/direct-joint-injection on any of the 5 surfaces) — tracking error sub-0.02deg at every point (mostly sub-0.005deg), zero `target_clamp_active`/`effort_clamp_active`, zero NaN/Inf, zero smoothness violations, propulsion left/right symmetric to <=0.11 RPM / <=0.0025 N across all 21 points.** Central-difference effective slopes (per rad, using ACTUAL achieved angle, not commanded) at +/-2/+/-5/+/-10deg: elevator `CL_delta_e_GZ`~0.0005-0.0007 (expected near-zero, no `CLde` term in the V1 model), `Cm_delta_e_GZ` -0.53 to -0.73 (closest to config `Cmde=-0.73` at the smallest/most-linear +/-2deg window); aileron `Cl_delta_a_GZ`~0.3063, `Cn_delta_a_GZ`~+0.0012, `CY_delta_a_GZ`~0.0255 (all three highly consistent across window sizes); rudder `CY_delta_r_GZ`~0.0849, `Cn_delta_r_GZ`~-0.0250, `Cl_delta_r_GZ`~0.00067 (also highly consistent across window sizes). **6 short (~2.5s) free-flight runs** (base_link fully unheld, no controller, other 4 surfaces held at trim/neutral via the real actuator) at Delta=+/-5deg per channel: all showed the expected-direction initial rate response (elevator: q flips sign with Delta_e sign, consistent with the model's documented "+delta_e -> nose-down" static-group convention; aileron: p/beta/r fully antisymmetric between +5/-5deg, confirming adverse-yaw coupling; rudder: r/beta antisymmetric with a small p coupling) — `any_nan=False` in all 6. **One test-harness bug found and fixed this pass (not an aero/actuator code defect):** `aero_lib.hold_step()`'s pure-P (no integral) angular rate hold left a persistent residual body rate under the large +/-10deg aero moments this script measures (invisible in every prior +/-10deg-scale test in this suite, which never computed fine coefficient slopes) — this fed into the SAME channel's own Clp/Cnp (aileron) and Cmq/CLq (elevator) coupling terms and, at the original kp_ang=150, produced a WRONG-SIGNED `Cn_delta_a_GZ` (-0.0009/rad vs the config's `Cnda=+0.00144/rad`). Fixed by raising the quasi-static portion's `hold_step` angular gain (test-only Python controller parameter, not a physics parameter) from 150 to 1500 (2000 was tried and triggered a genuine `ODE INTERNAL ERROR 1` physics-engine numerical blow-up, so 1500 was kept with margin) — confirmed by a direct 150-vs-1500 comparison that this recovers the correct sign/closes most of the gap for aileron's `Cn`/`Cl`, tightens elevator's `Cm_delta_e_GZ`, and does not materially change rudder (whose induced moments were always smaller). Full numeric evidence and derivation of this finding are in the script's own module docstring. No aircraft physics parameter (aero coefficient, control-sign mapping, `control_deflection_clamp_deg`, actuator `max_rate`/`max_effort`/`kp`/`kd`/`ki`/`sp_weight_b`, motor/prop constant, mass/CG/inertia, hinge geometry) was modified anywhere in this pass. New script: `tests/gazebo/scripts/test_control_authority_effectiveness.py` (reuses `actuator_lib.py`/`aero_lib.py` unchanged). Results: `tests/gazebo/results/control_authority_{quasi_static,free_flight}_{log.txt,result.json}`. The XFLR5-comparison/classification pass and `validation`'s independent review both remain outstanding next steps, not part of this pass.

**Update, 2026-08-26 (`HIGH_DEFLECTION_CONTROL_AERO_IMPLEMENTATION` confirmation, `gazebo-testing`):** small, targeted live-Gazebo confirmation (not a full regression) of `aerodynamics`' new bounded piecewise-linear wide-deflection control-surface lookup (`plugins/aerodynamics/AeroModel.hh`/`AerodynamicsSystem.cc`, elevator/aileron/rudder, +/-45deg domain, replacing the old generic +/-10deg linear-clamp model; standalone self-test already 31/31 PASS). Reused `test_control_authority_effectiveness.py`'s live actuator->joint->aero chain and `hold_step`/`KP_ANG_QSTATIC=1500` technique unchanged. **Part A (11 absolute-target points per surface: 0/+-2/+-5/+-10/+-25/+-45deg, 33 quasi-static runs total) — ALL PASS**, measured delta-from-baseline vs. the wide-deflection source table matching almost exactly at every breakpoint (e.g. elevator Cm at +45deg: measured -0.7834/rad-scaled vs table -0.7843; aileron/rudder Cl/Cn/CY/CD/CL/Cm likewise sub-5%). CD comparisons for elevator/aileron required accounting for a genuine `k*CL_total^2` cross-term (CL_total itself shifts with `dCLeCtrl`/`dCLaCtrl`) on top of the table's own `dCD` entry — confirmed exactly (0.07587 predicted vs 0.07588 measured at elevator +45deg) once included; a naive "table-diff-only" comparison would have shown a false ~60% gap. **Part B (symmetry at +-10/+-25/+-45deg) — ALL PASS** (odd Cl/Cn/CY for aileron, odd CY/Cn for rudder; even CL/CD/Cm for aileron, even CD for rudder). **Part C (central-difference small-signal recovery at +-2/+-5/+-10deg) — all close**: `CL_delta_e`~-2.9%, `Cm_delta_e`~-0.2%, `Cl_delta_a`~-0.4%, `CY_delta_a`~+2.4 to +5%, `CY_delta_r`~-0.3%, `Cn_delta_r`~-0.2%, `Cl_delta_r`~-4.2% (recovers ~+0.00067/rad vs the kept-old +0.0007/rad, as expected) all within normal measurement noise; `Cn_delta_a` measured -18.8 to -20.4% low, plausibly the same Clp*pHat-residual-body-rate contamination mechanism this suite's prior `CONTROL_AUTHORITY_EFFECTIVENESS_VALIDATION` pass already disclosed for this exact small/tertiary term (not re-litigated/fixed here). **Part D (attempt to exceed +-45deg) — the aero model's own domain-edge behavior IS confirmed** (Part A's own exact +-45deg commands, sent AT not beyond the boundary, settled to <0.04deg of +-45 and matched the table almost exactly) **but a genuine, reproducible `OBSERVED_ISSUE` was found in the actuator's response to a PERSISTENT out-of-range raw command** (elevator/aileron/rudder all commanded 60deg-equivalent): the Stage-1 mechanical clamp correctly saturates the SETPOINT to exactly +-45deg (verified via direct diagnostics probe), but the ACTUAL joint angle gets stuck ~29-33% of the travel range short of that clamped setpoint (e.g. elevator: 32.12deg of 45) and stays there for the full multi-second settle window — not a transient, and not a torque-authority limit (`effort_clamp_active` never true). Root cause, read directly from `ActuatorModel.hh::PidEffort()`: integral accumulation is frozen whenever `targetClampActive` is true, which for a *persistent* over-range raw command never clears, silently reintroducing the P(D)-only steady-state droop the 2026-08-24 gravity-droop/integral-action fix was specifically added to eliminate. Reported as evidence for `controls-integration`/`validation` — no actuator parameter or logic touched by this test. **Part E (short ~2.5s free-flight sanity, aileron delta_a=+5deg small / +25deg large absolute, from the existing powered trim point) — PASS**: actuator moves the real joint, aero reads the real joint position, roll response is correctly signed and scales consistent with the nonlinear table (p~+50deg/s at t=0.3s for +5deg vs. p~+248deg/s for +25deg, roughly tracking the table's Cl(25)/Cl(5)~4.98 ratio), no NaN/Inf/crash in either case, propulsion stayed stable (small RPM/thrust left-right asymmetry appeared as the real, expected consequence of the developing roll rate, not a numerical artifact). No aero coefficient, actuator parameter, or lookup table value was modified anywhere in this script. New script: `tests/gazebo/scripts/test_high_deflection_control_aero.py` (reuses `actuator_lib.py`/`aero_lib.py`/`propulsion_lib.py` unchanged; independently re-parses `aero_v1_config.yaml`'s `control_surface_lookup` block read-only for expected-value cross-checking, since `aero_lib.py`'s existing pure-Python mirror still implements the old pre-lookup model and was intentionally left untouched). Results: `tests/gazebo/results/high_deflection_control_aero_{log.txt,result.json}`. `validation`'s independent review and the Part-D actuator finding's triage both remain outstanding next steps.

**Update, 2026-08-27 (`UPDATED_POWERED_TRIM_AND_HIGH_DEFLECTION_FLIGHT_VALIDATION`, `gazebo-testing`):** the prior `HIGH_DEFLECTION_CONTROL_AERO_IMPLEMENTATION` pass changed the small-signal/lookup control coefficients (new `CL_delta_e=+0.414/rad` term, `Cm_delta_e` -0.73->-1.000/rad), leaving the existing powered trim (throttle=0.4915, elevator +5.50deg physical, V=18.165 m/s) stale (lift deficit + stronger nose-up tendency at that same command, i.e. My more negative — corrected wording per `validation`'s 2026-08-27 review; this project's convention is negative My = nose-up, see `AeroModel.hh`'s own sign-resolution comment). This pass re-trims from scratch and validates free-flight/high-deflection dynamics under the new model. **New trim search: 35 candidates** (coarse grid centered by an offline analytical solve of the unchanged Lift=Weight/moment-balance equations against the new lookup — not a physics change, a test-planning aid — then the same coarse->refine1->refine2->round3-settled-tail-check->round4-throttle-extrapolation methodology as `test_powered_trim_search.py`, reused verbatim) converged on **throttle=0.5010, elevator physical +4.50deg L/R (elevator_aero=-4.50deg), V=18.166 m/s, alpha=2.473deg** — CL=0.63955, CD=0.05637, Lift=58.35N vs Weight=58.86N (ratio 0.992), settled tail vertical-speed +/-0.009 m/s, My(recomputed)=-0.354Nm. Vs. the OLD trim: elevator shifted from -5.50deg to -4.50deg aero (+1.0deg, smaller magnitude) and throttle rose slightly (0.4915->0.5010) — both directly explained by the new `CL_delta_e`/`Cm_delta_e` lookup entries (config's own `dCL(-5deg)=-0.03608`, `dCm(-5deg)=+0.08710`): the old, larger-magnitude deflection had become needlessly lift-reducing and more nose-up-inducing under the new model. **Part 3 (real-actuator trim-hold check) — PASS**: Lift/Weight=0.9917, T/D=1.081 (mild, expected given the fixed reference-V hold — Part 1's own free-release search already confirms the true equilibrium V is very close by, ~+0.2 m/s airspeed drift over 12s), RPM/thrust left-right symmetry to <=0.0001 RPM / <0.00001N, and a +/-1/+/-2deg elevator sweep straddling the -5deg breakpoint showed **perfectly smooth CL/Cm slopes (~+0.00702/deg, ~-0.01741/deg) across the breakpoint — no discontinuity/clamp artifact**. **Part 4 (~25s genuine free 6-DOF flight, real actuator, propulsion live, no autopilot) — PASS**: a clean, lightly-damped phugoid (V 17.4-18.7 m/s, pitch -6 to +0.3deg damping toward ~-3.7deg, alpha bounded 2.47-2.75deg, roll <=0.10deg, yaw drift <=0.7deg over 25s, altitude +5.9m climb consistent with the mild T>D bias above, RPM/thrust symmetric throughout) — no divergence, no NaN, no asymmetry. **Part 5/6/7 (+/-15deg and +/-25deg short ~1s pulses, all 3 surfaces, real actuator, from the new trim) — no NaN in any of the 12 cases, smooth 10-25deg-region transition confirmed for all 3 surfaces (no flat/clamped old-+/-10deg-artifact), control-drag grows qualitatively correctly (aileron largest: +0.022 to +0.051 CD at +/-15/25deg vs elevator/rudder's smaller growth), anti-windup **NON_BLOCKING/NOT_TRIGGERED** (all pulses well within +/-45deg, matching the prior stage's own finding that only a *persistent* out-of-range raw command triggers it).** Elevator +/-15/25deg pulses showed strong, bounded pitch authority consistent with the new larger `Cm_delta_e` (pitch swings up to 42deg, alpha up to ~16deg within 1s, CL correctly smooth-saturated below `CLmax`=1.42 throughout, never diverging) — triaged as **(a) expected strong control authority**, not a defect. Aileron +/-15/25deg pulses showed very fast roll authority (roll-rate rise time ~0.1-0.2s, consistent with the documented roll-subsidence mode tau~0.106s) reaching 145-180deg of roll and 15-32deg of sideslip within 1s — also triaged as **(a) expected strong authority**, with an `OBSERVED_ISSUE` noted for `aerodynamics`: the V1 model applies `CYb`/`Clb`/`Cnb` as unbounded linear terms in beta (no saturation/domain-bound analogous to the alpha `high_alpha_limiter` or the control-deflection lookup's own +/-45deg bound), so beta excursions this large are a genuine small-disturbance-model extrapolation concern surfaced by this pass, not something this script modified or fixed. **Part 6 raw evidence (data only, no classification, per this task's own instruction)**: rudder +15deg -> Mx=-0.056 (early), initial p-trend -5.85 deg/s^2; rudder -15deg -> Mx=+0.057, p-trend +5.91 deg/s^2; rudder +25deg -> Mx=-0.059, p-trend -8.53 deg/s^2; rudder -25deg -> Mx=+0.059, p-trend +8.59 deg/s^2 (fully antisymmetric, as expected). **Important caveat disclosed alongside this raw data**: an order-of-magnitude decomposition of the measured `Cl` shows the dominant contributor at these samples is `Clr*rHat` (the yaw-rate-induced-roll cross term, unchanged/non-disputed, since rudder inherently and immediately induces a yaw rate `r` that couples into roll) rather than the disputed `Cldr` term itself — this dynamic-pulse technique does **not** cleanly isolate `Cldr`'s sign the way a p=q=r=0-held quasi-static measurement would; the raw numbers above are reported as requested, but should not be read as a clean direct measurement of `Cldr` alone. No aircraft physics parameter (mass, CG, inertia, aerodynamic coefficient, lookup table, control authority, motor/prop constant, actuator parameter) was modified anywhere in this script — one pre-existing cosmetic test-harness quirk (inherited unchanged from `test_powered_trim_search.py`'s own pattern: the round-3/4 log line's `net_My_est` display field only exists on the round-1/2 candidate dicts, so it prints as `nan` for round-3/4's own freshly-created result dicts — display-only, does not affect candidate ranking/selection) was observed and left as-is (harmless, matches the already-established original script's identical behavior). New script: `tests/gazebo/scripts/test_updated_powered_trim_high_deflection.py` (reuses `actuator_lib.py`/`propulsion_lib.py` unchanged and `aero_lib.py`'s `hold_step`/`DiagSubscriber`/topic-path constants only — NOT its stale pre-lookup `compute_aero()`/`load_config()`, which would `KeyError` on the now-removed `control_deflection_clamp_deg` key; includes a fresh, minimal, read-only mirror of the CURRENT `AeroModel.hh::ComputeAero()` lookup architecture for cross-checking/Mx-My-Mz recomputation, following the same pattern `test_high_deflection_control_aero.py` already established). Results: `tests/gazebo/results/updated_trim_search_{log.txt,result.json}`, `tests/gazebo/results/high_deflection_flight_{log.txt,result.json}`. `validation`'s independent review, the beta-saturation `OBSERVED_ISSUE`, and the still-open `Cl_delta_r` sign question all remain outstanding next steps for `aerodynamics`/`validation`, not part of this pass.

**Update, 2026-08-27 (same day, `UPDATED_POWERED_TRIM_AND_HIGH_DEFLECTION_FLIGHT_VALIDATION` `validation`-review fix-up, `gazebo-testing`):** `validation` found one MAJOR, fixable test-harness gap and two MINOR wording nits in the pass above (Parts 1-4, the trim search, and the free-flight run were independently confirmed correct and were **not** re-run). **MAJOR (fixed):** `run_pulse_test()`'s Parts 5/6/7 never subscribed to the live `FalconV2Aerodynamics` diagnostics topic — every CL/CD/CY/Cl/Cm/Cn/Mx/My/Mz value for all 12 pulse points came solely from this script's own `predict_aero()` Python mirror, contradicting the module docstring's claim of a live cross-check (which Part 1/Part 3 already did correctly). Fixed by adding `aero_lib.DiagSubscriber()` to `run_pulse_test()`. **A naive window-average comparison initially showed apparent mismatches up to ~39% relative diff** (aileron CY/Cn/Mz) — traced, by direct tick-by-tick tracing, to a **20Hz live-topic publish-rate staleness artifact**, not an implementation bug: the live diagnostic only updates every ~50ms while e.g. the aileron's roll rate is building at >500deg/s², so a window spanning several publish periods mixes several genuinely-different, stale live messages against a per-tick mirror average. Fixed methodologically with a new **timing-paired comparison** (`paired_live_ticks()`/`paired_live_mirror_diff()`, new module-level helpers) that resamples down to the live topic's own true update cadence and compares tick-for-tick — **all 12 pulse points now `CONFIRMS_MIRROR`** (0 genuine mismatches; one apparent elevator `My` outlier at the raw-mean level was traced to two individually-explainable ticks — a moment zero-crossing where a tiny absolute diff produces a meaningless large percentage, and one fast-transient tick — fixed by switching the pass/fail basis from a mean-of-relative-diffs to a floor-aware per-tick fail-fraction, matching the `TOL`-dict pattern `test_high_deflection_control_aero.py` already established for exactly this near-zero-crossing problem). Both `updated_trim_search_result.json`/`log.txt` (Part 4 preserved verbatim, untouched) and `high_deflection_flight_result.json`/`log.txt` (Part 5/6/7 sections regenerated with the live cross-check included) were updated; a `--part567-only` re-run mode was added to the script so Parts 1-4's already-confirmed-correct, expensive-to-reproduce results did not need to be re-executed. **MINOR (fixed):** (1) "nose-down"/"nose-down-inducing" wording describing the OLD-trim-vs-NEW-model My shift was backwards (corrected to "nose-up"/"nose-up-inducing" per this project's negative-My-is-nose-up convention, `AeroModel.hh`'s own sign-resolution comment) — fixed in both this file's prior 2026-08-27 entry above and the script's own docstring/explanation text; (2) the ±25° EARLY-window (t=0.08-0.18s post-pulse-onset) beta values, recomputed directly for `aerodynamics`' `AERODYNAMICS.md` §21 correction: aileron +25°=+0.5344deg, aileron -25°=-0.5718deg, rudder +25°=+0.4692deg, rudder -25°=-0.5070deg (vs. the ±15°-only values of ~0.36-0.43deg already cited there — consistent with `validation`'s independently-recomputed ~0.47-0.50deg range, confirming the ±25° figures are moderately higher, as expected, not a new finding requiring re-classification of anything already concluded). No aircraft physics parameter (aero coefficient, actuator parameter, propulsion parameter) was touched — this was purely a test-harness methodology/wording fix. Modified: `tests/gazebo/scripts/test_updated_powered_trim_high_deflection.py` (live diagnostics cross-check + timing-paired comparison + wording fixes), `tests/gazebo/README.md` (this entry + wording fixes in the entry above), `tests/gazebo/results/high_deflection_flight_{log.txt,result.json}` (Part 5/6/7 regenerated).

**Update, 2026-08-27 (`FLIGHT_ENVELOPE_VALIDATION`, `gazebo-testing`):** validated the CURRENT, already-implemented physics model (unchanged this pass) across an 8-point speed envelope (12.5/14/16/18.166/21/24/28/30 m/s) — a coverage/robustness check, not a re-tune. **115 trim candidates tested** (offline analytical solve per speed to center/seed a bounded Hooke-Jeeves-style "+"-pattern local search, marching outward from the already-validated 18.166 m/s reference trim in both directions per the task's explicit instruction — reused, not re-searched). **Critical finding, root-caused before any live run**: the offline solver, extended this pass to jointly solve alpha+delta_e against the REAL nonlinear lookup/saturation curve (not a linearization), found V=12.5 m/s is `AERODYNAMIC_NO_TRIM_MOMENT` — CL_required=1.36 is achievable (barely, via alpha~11.4deg, deep past `alpha_transition=9.25deg`, exploiting the documented no-real-stall smooth CL saturation) but NO elevator deflection within its CL-feasible range also zeros the pitching moment (elevator authority runs out before the moment does) — confirmed live: My=+1.55Nm (2.4x the next-largest residual of all 8 points), mean|q|=7.4deg/s during free release (vs <=2.7deg/s everywhere else). **Final classification: 12.5 m/s = `NO_VALID_TRIM` [`MODEL_PREDICTED_ONLY_NO_STALL_PHYSICS`]** — explicitly NOT evidence the real aircraft can fly this slow (master dataset's own Vstall~=12.24m/s / Vmin_safe~=14.69m/s remain the real-aircraft reference, cited not re-derived). **14/16/21/24/28/30 m/s = `MARGINAL_TRIM`, 18.166 m/s = `TRIM_FOUND`** (reference point independently re-confirmed: smallest My=-0.337Nm and mean|q|=0.82deg/s of all 8 points) — see `flight_envelope_result.json`'s `final_classification_reconciliation` key for the full per-speed reasoning, which combines the automated search-confirm metric, corrected actuator-hold Lift/Weight+Thrust/Drag, the analytical feasibility status, and Part-6 free-flight corroboration. **OBSERVED_ISSUE (test-harness, fixed within this pass, not a physics defect)**: the bounded local search's `candidate_score` prioritizes vz/lift balance over thrust=drag; at 12.5/14 m/s this metric was only weakly throttle-sensitive, so the search converged on a lift/moment-balanced elevator but a thrust-DEFICIENT throttle (T/D=0.42/0.44 as originally found). Re-solved via the same already-existing, unmodified thrust=drag root-finder (elevator untouched) and re-measured through the real actuator: T/D corrected to 0.998/0.997 at both speeds, confirming this was a search-convergence gap, not a propulsion capability limit. **Part 4 (high-alpha limiter static check, triggered by the low-speed region, 9 points from 7-20deg alpha)**: monotonic non-decreasing CL, C1-continuous (no slope kink across `alpha_transition`), no NaN, no sign reversal; a 0.16% momentary CL excess over `CLmax` at alpha=20deg is fully explained by the intentionally-unsaturated `CLq*qHat` rate term (a small residual pitch rate during the quasi-static hold), matching the documented "rate term added on top, unsaturated" architecture exactly — not a limiter defect. **Part 6 (3 free 6-DOF flights, ~14s each, LOW=14/NOMINAL=18.166/HIGH=24 m/s)**: all 3 **PASS**, bounded phugoid-like behavior, no NaN, no divergence (LOW: pitch start +0.18deg->end -5.65deg, max|pitch|=11.95deg reached during the transient, alpha 7.82-8.94deg; NOMINAL: pitch start +0.26deg->end -4.24deg, max|pitch|=6.06deg during the transient, alpha 2.47-2.72deg; HIGH: pitch start +0.42deg->end +2.94deg, max|pitch|=4.39deg during the transient, alpha -1.11 to -0.85deg, -11.4m/14s altitude drift) — all three max|pitch| values match `flight_envelope_result.json`'s own `summary.max_abs_pitch` field exactly; the start/end pair alone understates the mid-transient excursion since it omits the peak swing between release and settling, corrected here per `validation`'s review. **Part 7 (+/-5deg representative control-authority check, 3 surfaces x 3 speeds)**: dimensional moment authority (dMx/dMy/dMz) grows correctly with qbar~V^2 (e.g. aileron dMx: 2.80/4.80/9.50 N*m at 14/18.166/24 m/s) while the coefficient-level response (dCl/dCm/dCn) stays roughly consistent across speed, confirming the growth is expected V^2 dynamic-pressure scaling, not a derivative change. **Part 8/9 (actuator/propulsion monitoring, all 8 trim points + all control tests)**: actuator tracking error stayed sub-0.05deg everywhere (no `ACTUATOR_LIMITED_REGION`), zero `target_clamp_active`/`effort_clamp_active`; RPM stayed well within the 11500 RPM V1 safety cap (max 9491 RPM at 30 m/s) and current stayed well within the 65A limit at every point (no `PROPULSION_LIMITED` anywhere in the aerodynamically-feasible 7/8 points). No aircraft physics parameter (mass, CG, inertia, aerodynamic coefficient, control lookup table, control derivative, high-alpha limiter constant, propulsion parameter, actuator parameter) was modified anywhere in this script. New script: `tests/gazebo/scripts/test_flight_envelope.py` (reuses `test_updated_powered_trim_high_deflection.py`'s `predict_aero()`/`REF`/`get_model()`/`quat_rpy()`/`actual_deltas()` and `actuator_lib.py`/`aero_lib.py`/`propulsion_lib.py`'s existing primitives unchanged; generalizes the existing hold-then-release/quasi-static-actuator-hold/free-flight/pulse techniques to an arbitrary target speed instead of a single fixed one — no new methodology invented). Results: `tests/gazebo/results/flight_envelope_{log.txt,result.json}`. `validation`'s independent review remains an outstanding next step, not part of this pass.

**Update, 2026-08-27 (`ENGINE_OUT_AND_ASYMMETRIC_THRUST_VALIDATION`, `gazebo-testing`):** validated the CURRENT, unchanged twin-motor asymmetric-thrust/engine-out behavior (no propulsion constant, aero coefficient, control lookup, rudder derivative, actuator parameter, CG, inertia, or mass modified anywhere in this pass). **Part 0 (analytic sign pre-registration, no Gazebo): all 3 pre-registered predictions self-consistent**, then **independently confirmed live** in Part 1: symmetric thrust gives Mz_prop/Mx_reaction=0 (machine-zero, re-confirming `COUNTER_ROTATION_CANCELLATION_TEST`); LEFT_OUT (right motor alone) gives Mz_prop=+0.8346 N·m (nose-left, toward the dead engine) and Mx_reaction=+0.1306 N·m (roll right/left-wing-up); RIGHT_OUT is exactly mirror-antisymmetric (both to machine precision, 0.000% residual) — **0/3 disagreements**. **Part 1 (7-case static differential-thrust table)**: B/C, D/E, F/G pairs all mirror-symmetric to <1e-6 N·m; live-thrust r×F recomputation and the independent propulsion_lib.py Ct/Cp-mirror recomputation (fed live RPM) agree to <0.05%. **Part 2 (zero-rudder engine-out transient, ~5s, real spool-down via a throttle-command step — never an RPM override)**: failed-motor RPM decays smoothly (6087→428 RPM over 5s, max single-tick drop 229 RPM, no discontinuity) for both sides; natural (uncorrected) yaw drifts toward the dead engine (LEFT_OUT: −6.96° yaw over ~4.2s of free response ≈ drift toward the LEFT convention sign, RIGHT_OUT mirror +6.91°) and roll drifts in the pre-registered reaction-torque direction (LEFT_OUT +13.9°, RIGHT_OUT −13.8°) — both mirror-antisymmetric, matching Part 0/1. **Part 3/4 (targeted rudder-compensation search)**: LEFT_OUT needs +17.10° (residual Mz≈−0.0006 N·m), RIGHT_OUT needs −17.00° (residual ≈−0.0013 N·m) — mirror-symmetric to 0.1° / machine-zero Mz sum. **Part 5 (surviving-engine throttle adequacy)**: baseline 0.5010 throttle alone is insufficient (T/D=0.54); an offline pure-Python bisection (propulsion_lib.py's steady-state rotor-ODE solver, no Gazebo) predicts throttle=0.5614 restores T/D≈1.0, confirmed live (T/D=0.9995, RPM=6725/11500=58% of cap, current=18.95/65 A=29% of limit) — **classification: none of `PROPULSION_LIMITED`/`YAW_AUTHORITY_LIMITED` apply; adequate margin on both axes.** **Part 6 (single-engine quasi-static trim attempt, aileron neutral)**: Lift/Weight=0.992, T/D≈0.96–0.96, My≈−0.18 N·m, Mz≈0.001 N·m residual, Mx≈±0.234 N·m (reaction-torque-dominated, documented and left uncorrected — judged small relative to aileron authority per `CONTROL_AUTHORITY_EFFECTIVENESS_VALIDATION`'s own N·m-scale aileron figures, so aileron was correctly kept neutral per the task's own rudder-first instruction). **Part 7 (single-engine free 6-DOF flight, ~13s, real actuator, no autopilot)**: both sides **PASS_WITH_DRIFT** — no NaN, altitude drift only ≈−1.8 m, airspeed stable (+0.21 m/s drift), but roll and yaw drift meaningfully over the window (LEFT_OUT: roll →+18.4°, yaw →−6.96° at ~4s then continuing, RIGHT_OUT mirror) — **directly consistent with Part 6's uncorrected Mx residual and the Part 0/1/2 sign predictions**, i.e. the quasi-static rudder-only trim zeros Mz at the pinned (β=0,p=q=r=0) condition but does not fully arrest the slower roll/heading drift once genuinely free — reported as the real, expected consequence, not tuned away. **Part 8 (speed dependence, 16/24 m/s, rudder-requirement only, reusing `flight_envelope_result.json`'s already-validated trims read-only)**: required rudder shrinks with speed as expected (21.5°→17.1°→13.1° at 16/18.166/24 m/s) since aero rudder authority scales with qbar while Mz_prop is airspeed-independent (thrust-based only) — mirror-symmetric at both extra speeds. **Part 9 (rudder yaw-authority margin)**: no `LOW_RUDDER_MARGIN` (required rudder ≤21.5° at all 3 tested speeds, under the 25° flag threshold) and no `RUDDER_SATURATION_RISK` anywhere (max 21.5° vs ±45° mechanical limit). **Part 10 (actuator/numerical safety)**: zero NaN/Inf across all ~50 live runs, zero `target_clamp_active`/`effort_clamp_active` in the static suite, smooth (non-instantaneous) RPM decay confirmed both sides, no RPM sign anomaly (failed motor settles to a small ≈−29 RPM residual windmill state under the existing negative-current-clamp policy, not a spurious large-magnitude flip), no hidden controller/artificial wrench beyond the existing model. `Cl_delta_r` policy respected — engine-out roll response was not used to resolve it (still `+0.0007/rad`, `STILL_INCONCLUSIVE`, untouched). No `TEST_FAILED`; no genuine model limit blocked single-engine flight at the tested conditions. New script: `tests/gazebo/scripts/test_engine_out_asymmetric_thrust.py` (reuses `test_flight_envelope.py`'s already-established `ACT`/`AL`/`PL`/`REF`/`predict_aero()`/`get_model()`/`quat_rpy()`/`actual_deltas()`/`steady_state_rpm()`/`PCFG`/`APC_SLICES` unchanged — no new methodology invented, only independent-side-throttle generalization of the existing quasi-static-hold/free-6DOF-flight/real-actuator techniques). Results: `tests/gazebo/results/engine_out_asymmetric_thrust_{log.txt,result.json}`. `validation`'s independent review remains an outstanding next step, not part of this pass.

**Update, 2026-08-27 (same day, `ENGINE_OUT_AND_ASYMMETRIC_THRUST_VALIDATION` validation follow-up, `gazebo-testing`):** `validation` found a real MAJOR gap in the pass above's original ~13s Part 7 single-engine free-flight runs — both sides ended the window with roll/yaw STILL AT their window-max, with no persisted evidence of leveling off, so "bounded through 13s" was not actually the same claim as "bounded." Targeted follow-up (Parts 1/3/4/5/6/8/9/10 NOT re-run, loaded read-only from the existing result JSON): (1) extended the single-engine free-flight to ~29.1s (both sides, identical throttle/rudder/aileron-neutral conditions) and added a plateau-vs-continued-growth trend check; (2) persisted a decimated (50 Hz, 250-point) raw RPM time-series for the Part 2 engine-out transient (both sides) so the "smooth decay, not instantaneous" claim can be independently re-verified from the JSON alone. **Self-caught methodology bug found and fixed WITHIN this same follow-up pass, before reporting:** a first version of the trend check finite-differenced the Euler `yaw_deg` angle directly, which (a) wraps at ±180° (this run's own yaw crosses that boundary, e.g. −179.57°→+162.96° between consecutive samples, producing a spurious huge "rate") and (b) is the wrong question for yaw regardless — under ANY sustained nonzero turn rate the yaw ANGLE grows forever by definition (ordinary turning flight, not divergence). Fixed by computing the trend from the never-wrapping body rates (`p_deg_s`/`r_deg_s`) directly, comparing their mean over the last third vs middle third of the window, with a floor-aware bucket (`NEGLIGIBLE_RATE_SETTLED` when both means are already <1 deg/s, avoiding a meaningless ratio computed from near-zero noise — the same floor-aware pattern this suite already uses elsewhere for near-zero-crossing comparisons). **Corrected result: both sides genuinely BOUNDED at ~29s** — roll rate `NEGLIGIBLE_RATE_SETTLED` (bank angle converges to ≈21.4–21.8°, roll rate ≈0.15–0.2°/s, decaying, not growing), yaw rate `RATE_LEVELED_OFF_STEADY_NONZERO` (settles to a steady ≈∓9.1→∓10.0°/s turn rate, ratio≈1.10, i.e. leveled off, not accelerating) — i.e. the aircraft settles into a steady, mildly-banked turn under rudder-only compensation (a legitimate bounded engine-out flight condition, not a spiral divergence), mirror-symmetric between LEFT_OUT/RIGHT_OUT. This is classification **(a)** from the follow-up request, not (b). No aircraft physics parameter modified. New JSON keys (additive, nothing prior overwritten except `part2_transient`, re-run fresh with the same methodology plus the new decimated-series field): `part7_free_flight_extended_30s`, `followup_note`; `part2_transient` updated in place; `part7_free_flight` (original ~13s) and all other parts left untouched. New CLI mode: `test_engine_out_asymmetric_thrust.py --followup` (calls `main_followup()`, reuses `run_transient_engine_out()`/`run_single_engine_free_flight()` unchanged in mechanics, only the latter's step counts/telemetry-rate were made overridable parameters). **Separately observed (not caused by or edited by `gazebo-testing`)**: `docs/source_of_truth/aerodynamics/AERODYNAMICS.md` §23 was added concurrently by `aerodynamics`, citing this task's original (pre-follow-up) data; its qualitative verdict ("not diverging, executing the roll-damping-limited drift physically expected") is actually corroborated, not contradicted, by this follow-up's corrected 29s data - no conflict to resolve, flagged here only for traceability since it was written before this follow-up completed.

**Update, 2026-08-27 (`WIND_GUST_DISTURBANCE_MODEL_AND_VALIDATION`, `gazebo-testing`):** live-Gazebo validation of `aerodynamics`' new `plugins/wind/` `FalconV2Wind` System plugin, which publishes a composed steady + 1-cosine-gust wind VELOCITY (world frame, m/s, "velocity of the air mass" convention, see `docs/source_of_truth/environment/WIND.md`) on the EXISTING `/model/falcon_v2/wind` topic that `AerodynamicsSystem`/`PropulsionSystem` already correctly consume as `Vrel = Vbody - Vwind` (independently re-confirmed by direct grep of both files this pass — neither was modified). No aircraft physics parameter (mass, CG, inertia, aerodynamic coefficient, control lookup table, control authority, motor/prop constant, actuator parameter) was modified anywhere in this pass — the wind plugin is a pure additive disturbance-VELOCITY generator, never a direct force/wrench source. **Part 6 (no-artificial-force quick grep re-confirmation): 0 `AddWorldForce`/`AddWorldWrench` call sites in `plugins/wind/` outside comments — CONFIRMED.** **Part 1 (zero-wind regression)**: with the plugin loaded but never commanded, the published wind is exactly `(0,0,0)` at every tick throughout an 8s free-flight snippet; static-hold CL/CD/V/alpha at the current nominal trim (throttle=0.5010, elevator physical +4.50°L/R) match the 2026-08-26 reference (CL=0.63979 vs 0.63955, CD=0.05639 vs 0.05637, V=18.1656 vs 18.166 m/s, alpha=2.4728° vs 2.473°) to within a few parts in 10⁴ — **no regression**. **Part 2 (7-case static/held sign-test table, aircraft ground-velocity/attitude held near trim via the real actuator + `hold_step`, only the commanded wind varied): all 14 pre-registered sign predictions AGREE, 0 disagreements** — headwind(-5,0,0)/tailwind(+5,0,0) increase/decrease `V_rel` and `qbar` at the SAME ground speed (+4.99/+126.2 and -4.98/-95.6 respectively); crosswind toward +Y gives beta=-15.34° (negative), CY=+0.0354 (positive), Cn=-0.00953 (negative) exactly as predicted (mirror-antisymmetric for -Y); vertical +3/-3 m/s gives alpha +8.96°/-9.11° and CL +0.735/-0.866 (both same-sign as predicted). `qbar=0.5*rho*|V_rel|^2` scaling check: **max relative diff 0.000000 across all 7 cases** (machine-precision agreement, confirmed via an independent live-topic vs. commanded-wind mirror recomputation, not just self-consistency). Cl sign (not pre-registered) reported only: +0.0019 at +Y crosswind (mirror -0.0019 at -Y). **Part 3 (3 steady-wind ~13s free 6-DOF flights from trim, no autopilot)**: headwind/tailwind produce the expected natural climb/descend tendency at fixed ground-hold-released trim (alt drift +13.1m/-8.8m, both from a lift-vs-weight imbalance driven by the qbar/V_rel change at unchanged initial alpha, pitch transient up to 20°/14° while q-amplitude trend is `AMPLITUDE_DECAYING`, i.e. converging, not diverging); crosswind gives a decaying dutch-roll-like transient (roll to 4.45°, beta to 15.4°, p/q/r amplitude trends all `AMPLITUDE_DECAYING`, ratios 0.25/0.59/0.36). Ground speed and relative airspeed are reported and tracked SEPARATELY throughout (e.g. headwind: V_ground settles back toward ~18.15 m/s while V_rel stays offset by the commanded 5 m/s). **Part 4 (4 gust cases via `/gust_cmd`, 1-cosine, 2s duration, from trim, ~13s window each)**: longitudinal -5 m/s, lateral +5 m/s, vertical +3 m/s, plus one bonus longitudinal +8 m/s sanity point — all 4 **BOUNDED, 0 extension attempts needed** (p/q/r amplitude trend `AMPLITUDE_DECAYING` or `NEGLIGIBLE_AMPLITUDE_SETTLED` at the initial ~13s cutoff in every case — the methodology-lesson check from `ENGINE_OUT_AND_ASYMMETRIC_THRUST_VALIDATION` was applied up front this pass via an automatic re-run-with-extension helper, which never triggered). Gust timing/shape independently confirmed via a direct subscription to the real output topic (not just the intended command), matching the expected 1-cosine ramp/peak/decay envelope at the expected sim time. **Part 5 (actuator/numerical safety, all static+free-flight+gust runs)**: zero NaN/Inf anywhere, zero `target_clamp_active`/`effort_clamp_active`, elevator tracking error stayed bounded (~1.0-2.2° max — occurring during the initial servo slew-to-setpoint before release, not from wind/gust loading; tracking remained bounded and non-clamping throughout the actual wind/gust-affected window — no saturation, no anti-windup anomaly). No `TEST_FAILED`, no `OBSERVED_ISSUE` requiring escalation. New script: `tests/gazebo/scripts/test_wind_gust_disturbance.py` (reuses `actuator_lib.py`/`aero_lib.py`/`propulsion_lib.py`'s existing `hold_step`/`ActuatorCommander`/`ThrottleCommander`/`DiagSubscriber`/free-flight-release primitives unchanged; adds only wind-specific `WindCommander`/`WindTopicSubscriber` helpers and an amplitude-trend — mid-third vs last-third max|.| — boundedness classifier modeled directly on `test_engine_out_asymmetric_thrust.py`'s own rate-trend methodology, adapted to an oscillatory/phugoid-baseline airframe). Results: `tests/gazebo/results/wind_gust_disturbance_{log.txt,result.json}`. `validation`'s independent review remains an outstanding next step, not part of this pass.

**Update, 2026-08-27 (`SENSOR_MODEL_AND_ARDUPLANE_SITL_PREPARATION` live validation, `gazebo-testing`):** live-Gazebo validation of `controls-integration`'s new sensor suite (native IMU/GPS(navsat)/barometer(air_pressure)/magnetometer System sensors + the custom `FalconV2Pitot` plugin, see `docs/source_of_truth/sensors/SENSORS.md`), against the new `tests/gazebo/worlds/falcon_v2_sensors_selftest_world.sdf` world (unmodified). **IMU: PASS** — static/at-rest-under-gravity specific force `(-0.0014, ~0, 9.754)` vs documented `(0,0,+9.81)` convention (99.4% match, residual explained by `hold_step`'s finite proportional gain, not a sensor defect); a known constant-yaw-rate case (`r_target=+0.3rad/s` via `hold_step`'s angular target, no ArduPilot involved) measured `angular_velocity.z=0.30072rad/s` (+0.24%). **GPS: PASS** — free-fall and powered-forward-trim cases both confirm the trivial ENU identity mapping (`velocity_east/north/up` vs true world `Vx/Vy/Vz`) to <0.5% at steady flight, sign/magnitude/finiteness all correct; a small consistent `+0.1m` `gps_alt` offset noted as INFO only. **Barometer (`BARO_ALTITUDE_RESPONSE_REVIEW_REQUIRED` open finding): RESOLVED AS BENIGN, PASS** — a 40m continuous free-fall regression (150 timing-paired samples) matched the task's own ISA formula to 0.3% slope/~1Pa absolute, and an independent two-point spawn-altitude comparison (Z=10m vs Z=300m, 290m apart) matched ISA-predicted delta to within 0.2Pa — directly contradicting the implementer's single prior smoke-test sample, diagnosed as a probable one-off startup-timing artifact of a single manual `gz topic echo`, not a persistent defect. **Magnetometer (`MAG_FIELD_SOURCE_REVIEW_REQUIRED` open finding): CONFIRMED REAL, `TEST_FAILED`** — a new field-scale live test (new world `tests/gazebo/worlds/falcon_v2_sensors_selftest_altmag_world.sdf`, an exact copy of the selftest world with `<magnetic_field>` scaled 100x) showed the live magnetometer's `field_tesla` magnitude is **exactly unchanged** (ratio 1.0000) despite the 100x world-field change — decisive evidence the native `gz-sim8` Magnetometer system is not consuming the declared world field at all in this build (constant `|field|=0.32` regardless of heading/position/declared field; body-frame ROTATION with yaw is independently confirmed correct, only the magnitude/source is wrong). Reported as `TEST_FAILED`, suspected subsystem the native `libgz-sim8-magnetometer-system.so` binary (no source available to inspect further) as integrated by `controls-integration`; not fixed by this pass (hard constraint against tuning/fixing sensor code in this role). **Pitot (priority check): PASS, all 4 required live wind cases** at the nominal trim (throttle=0.5010, elevator=+4.50°, V≈18.166m/s) — zero-wind reads 18.165m/s (matches trim V), +5m/s headwind increases reading by +4.987m/s, +5m/s tailwind decreases it by -4.982m/s, a +5m/s crosswind reads 18.848m/s matching its documented `|V_point-V_wind|` definition to <0.04% — and independently cross-checked against `AerodynamicsSystem`'s own diagnostics topic `V` field (not just the pitot's own math mirrored back at itself) at ≤0.003% relative agreement in all 4 cases, per the task's explicit cross-check requirement. New script: `tests/gazebo/scripts/test_sensor_model_live_validation.py` (reuses `actuator_lib.py`/`aero_lib.py`/`propulsion_lib.py`'s existing `hold_step`/`ActuatorCommander`/`ThrottleCommander`/`DiagSubscriber` unchanged; adds a generic `RawSub` gz-transport wrapper for IMU/NavSat/FluidPressure/Magnetometer/Double message types and a timing-paired true-state-vs-sensor-message sampling technique for the baro/GPS continuous-motion cases). Results: `tests/gazebo/results/sensor_model_live_validation_{log.txt,result.json}`. Full report: `docs/test_results/2026-08-27_sensor_model_sitl_preparation.md`. No aircraft physics parameter modified anywhere in this pass. `validation`'s independent review remains an outstanding next step, not part of this pass.

**Update, 2026-08-28 (`ARDUPLANE_BASIC_CLOSED_LOOP_FLIGHT_VALIDATION`, `gazebo-testing`):** first-ever attempt at a closed-loop ArduPlane FBWA-controlled airborne test. **Result: `TEST_FAILED` / `NOT_READY` — 0 of the required 3 flights attempted, no flight data fabricated.** The prescribed pre-flight sequence's airborne "real force-based velocity ramp (0 → 18.16 m/s trim)" step never completes: the aircraft diverges into an extreme, unphysical pitch excursion (pitch −54.6° within t≈0.35s, vx=48.4 m/s / vz=24.9 m/s — far beyond what the ≤35N applied force could produce) **before ArduPlane is ever switched to FBWA**, reproducing even under a zero-reactive-feedback, pure-deterministic-feedforward control configuration (constant acceleration in X, exact gravity compensation in Z, zero torque) — decisive evidence this is a genuine aerodynamic-model low-airspeed/high-relative-alpha edge-case sensitivity during a from-rest acceleration transient, not a controller-tuning artifact of this agent's own ramp implementation (7 independent controller-design configurations tried and eliminated, see the full report). **Three real test-methodology findings were made and fixed along the way** (all `gazebo-testing`-owned, none an aircraft-physics change): (1) the stock `gz-sim-apply-link-wrench-system` applies `wrench.force` at the LINK ORIGIN by default, not the COM — since `base_link`'s CG is offset from its origin (`(0.168309,0,0.100000)`, matches CLAUDE.md), an uncorrected hold force induced a large spurious `r×F` moment (~80° pitch drift in 0.15s); fixed via `wrench.force_offset`=CG. (2) `World::SetPose` (`/world/<world>/set_pose`) does **not** reset the body's velocity — confirmed decisively (7 consecutive clean small-jump teleports vs. repeated large bogus post-teleport velocity when any gap existed before force compensation); any gap between a successful teleport and applying gravity-compensating force leaves the aircraft in real, uncompensated free-fall for that whole gap. (3) A tight active-polling loop against this stage's own `OdomSub`/`PoseSub` helpers correlated with intermittent bogus velocity readings that a passive sleep-based polling pattern never reproduced (suspected GIL/transport-callback contention, not proven at the C++ level) — mitigated via passive polling throughout. With all three fixed, PHASE 2 (teleport+verify) passes cleanly and reproducibly every time; PHASE 3 (the force ramp) is the real, remaining blocker. New world: `tests/gazebo/worlds/falcon_v2_ardupilot_basic_closed_loop_flight_world.sdf` (additive derivative of the grounded test world + world-scoped `ApplyLinkWrench` + per-model `OdometryPublisher`). New script: `tests/gazebo/scripts/test_ardupilot_basic_closed_loop_flight.py`. Results: `tests/gazebo/results/ardupilot_basic_closed_loop_ramp_{log.txt,result.json}`. Full report: `docs/test_results/2026-08-28_ardupilot_basic_closed_loop_flight_validation.md`. No aircraft physics parameter modified anywhere in this pass. **Routed to `aerodynamics`** (aero-model low-airspeed/high-alpha-transient edge case, the actual blocker) **and `validation`** (independent review of this finding, required before any further flight-campaign attempt).

**Update, 2026-08-28 (same day, `ARDUPLANE_BASIC_CLOSED_LOOP_FLIGHT_VALIDATION` methodology-correction + required 3-flight campaign, `gazebo-testing`, supersedes the 2026-08-28 update above):** the `TEST_FAILED`/`NOT_READY` result above is **superseded**. A follow-up pass replaced the original force-ramp mechanism with `aero_lib.hold_step()`'s own proven fixed-target technique (`phase3_hold_to_trim`), found and fixed 3 more test-harness bugs (`/wrench/persistent` accumulates rather than replaces — the likely dominant cause of the original "extreme pitch excursion"; `kp_ang=400` is unstable at this external transport loop's achievable sample rate, `kp_ang=100` converges cleanly; an unguarded post-teleport velocity glitch could crash the physics engine, fixed via a `VEL_SANITY_MAX_MS` sanity clamp; PHASE 2 needed angular-rate damping too), and achieved a clean, reproducible `PHASE3_4_PASSED_PRECONDITION_MET` — the aircraft releases from a real, force-based hold-to-trim state into a stable, bounded, ArduPlane-FBWA-controlled 10s window. This same pass then ran the actual 3-required-flight campaign (plus one optional combined-axis flight) using that proven mechanism, each from an independent fresh `gz sim`+`arduplane` launch: **`FLIGHT 1` (neutral stabilization, 20s) — stable**, roll near-zero throughout, pitch shows one real transient swing then decays smoothly to a small residual by t≈15–20s (no oscillation), airspeed/altitude bounded and non-divergent. **`FLIGHT 2` (roll commands, ±10° then ±20°) — `ROLL_CLOSED_LOOP: PASS`, `USABLE_INITIAL_PID`**: clean, monotonic, 0% overshoot, ~1.9s rise time independent of commanded magnitude, peak rate scales linearly with command, correct sign both directions (right command→right roll, confirmed against the FLU right-hand-rule derivation). **`FLIGHT 3` (pitch commands, ±5° then ±10°) — `PITCH_CLOSED_LOOP: MARGINAL`, `MARGINAL_INITIAL_PID`**: correct sign both directions (nose-up/nose-down), no saturation, never diverges, but the attitude response is confounded by a real, slow (~7–8s-period), lightly-damped longitudinal (phugoid-class) mode that this campaign's 3–4s command cadence repeatedly re-excites before it can settle — confirmed via altitude/airspeed co-oscillation in the classic phugoid pattern and contrasted against Flight 1's own undriven 20s window, which shows the SAME mode genuinely damping out when left alone; the underlying elevator actuator itself always tracks commands correctly and immediately. **Optional `FLIGHT 4` (combined ±15° bank + +5° pitch) — stable**, both axes tracked correctly and recovered cleanly together (one `PHASE 2` transport-flake retry needed, same already-documented §5.3 intermittent-teleport-velocity-reading issue, resolved by a fresh process pair, no code change). **CRITICAL sign/frame cross-check: PASS, all 4 flights** — `roll_mav≈+roll_gz` and `pitch_mav≈-pitch_gz` hold to <0.02–0.24° at every sample (matching the model's own documented FLU→FRD 180°-about-X body-frame transform exactly), no reversed axis. **Zero actuator/control-surface saturation at any layer, any flight** (100% of samples, all 5 surfaces, in the 0–10° HIGH_CONFIDENCE tier). **`THROTTLE_PROPULSION_INTEGRATION: PASS`** — twin-engine master-throttle+rudder-differential mixing (real ArduPlane `RUDD_DT_GAIN` mix, not independent RC5) stayed symmetric to ≤0.001 throttle-units across all 2154 combined samples. Airspeed/altitude monitored diagnostically only (never tuned around): a small, steady, non-accelerating ~0.5–0.8 m/s sink rate was consistent across all 4 independent flights, most plausibly an uncalibrated linear RC3-throttle-mapping bias (ASSUMPTION, not independently calibrated) given FBWA has no TECS/altitude-hold — routed to `propulsion`/`controls-integration` as a characterization note, not a defect. No aircraft physics parameter (mass, CG, inertia, aerodynamic coefficient, control authority, motor thrust, PID gain) was modified anywhere in this pass. New script: `tests/gazebo/scripts/test_ardupilot_basic_closed_loop_flight_campaign.py` (imports `test_ardupilot_basic_closed_loop_flight.py`'s proven `phase1`–`phase3` functions unmodified). Results: `tests/gazebo/results/ardupilot_flight{1,2,3,4}_result.json`. Full write-up: `docs/test_results/2026-08-28_ardupilot_basic_closed_loop_flight_validation.md` §10 (precondition fix) and §11 (this campaign). **Routed to `aerodynamics`/`controls-integration`** (whether the lightly-damped longitudinal mode's response to sub-period command cadence warrants a dedicated, slower-cadence characterization test before any future pitch-PID tuning pass) **and `validation`** (independent review of both §10 and §11, including the still-open, explicitly-not-resolved-here near-zero-airspeed/undamped-regime question flagged in §10.7).

**Update, 2026-08-28 (`ARDUPLANE_LONGITUDINAL_EQUILIBRIUM_AND_SINK_ROOT_CAUSE_VALIDATION` — Steps 2/3/5, `gazebo-testing`):** MEASURE-AND-PROVE root-cause of the ~0.55 m/s steady FBWA sink seen across every prior closed-loop flight. No aero coefficient/table, propulsion parameter, PID, `falcon_v2_sitl.parm`, actuator/sign mapping, joint limit, plugin source or SDF was modified — new test scripts + raw capture only, per the `controls-integration` Step-2 spec. **Part A/B — pure-Gazebo controller-independent harness** (`gz.sim8.TestFixture`, 1 ms fixed step, `falcon_v2_freefall_world.sdf`, `GZ_SIM_SYSTEM_PLUGIN_PATH` hard-set to prop+aero only so `ArduPilotPlugin`/`FalconV2Actuators`/wind/pitot/mag all fail to load and no bridge exists; elevator held rigid by direct-ECM pin; `AL.hold_step` 2 s IC hold then 60 s free release + linear-trend+damped-sinusoid fit). Model weight **queried live** = Σ(8 links·g) = **6.00000 kg → 58.860 N** (not assumed). **SIGN, probed and documented** (`scratchpad/pitchprobe.py`): `gz.math7 Pose3d` / `quat_rpy` Euler pitch is **NOSE-DOWN-POSITIVE** for this FLU world (a +8.59° pitch arg puts the nose world-Z at −0.149) — physical nose-up pitch = −(gz pitch); harness spawns at `PITCH0 = −α_guess` and reports physical pitch throughout. Aero mirror (`predict_aero`/`load_aero_ref` imported unmodified from `test_updated_powered_trim_high_deflection.py`) reproduces the live plugin CL/CD/Cm to **machine precision** (mean|Δ| ≈ 3·10⁻⁹, n=600 tick-paired). **Part C.1 — reference point (V=18.166, throttle=0.5010, elevator +4.50°) in pure Gazebo does NOT sink: it is a stable, energy-balanced equilibrium that CLIMBS at +0.235 m/s (γ=+0.75°), driven by a +0.78 N thrust excess** (T−D = +0.780 N = W·sin(0.75°) to 0.01 N; L/W 0.995; γ(world_vz)=+0.7517° vs γ(θ−α)=+0.7518°, consistency 6·10⁻⁵°; My_aero −0.223 N·m, net_My −0.063 N·m; q̄=+0.002°/s, V̇≈0, any_nan false). Measured **phugoid: period 8.7–8.9 s, ζ ≈ 0.20–0.22** (world_vz / V / θ fits all agree). **Part C.2 — bounded single-variable sweep (converged on iteration 1: 8 runs + 1 confirm, inside the 15–25 budget).** True steady-**level** point **(V\*, throttle\*, elevator\*, pitch\*, α\*) = (18.16 m/s, 0.4957, +4.09° physical, +2.49° nose-up, +2.47°)**: world_vz +0.0048 m/s, all 14 Part-D acceptance criteria **PASS**. Δ from the current reference: Δthrottle −0.0053, Δelevator −0.41°, ΔV≈0 — **not "meaningfully different"** per the spec's thresholds (independently matches `propulsion`'s "T=D at V=18.166 needs throttle ≈ 0.496"). **Part C.3 — FBWA×2 + MANUAL cross-check (fresh `gz sim`+gdb-`arduplane` pair per flight, `base` PHASE1-4 + `campaign` reused verbatim, `phase3_hold_to_trim` runtime-retargeted to the star point):** FBWA @ throttle\* (RC3=1496) → sink **0.598 m/s**; FBWA @ original 0.5010 (RC3=1501) → sink **0.551 m/s** (reproduces the prior 0.55 m/s exactly — methodology validated); **MANUAL @ elevator\* (RC2=1536) / throttle\* (RC3=1496) → LEVEL, +0.014/−0.011 m/s**, pitch_phys +2.52°, V 18.11 m/s — matching the pure-Gazebo equilibrium. FBWA's `NAV_CONTROLLER_OUTPUT.nav_pitch` demand is **0.000°** at neutral stick and it holds pitch_phys ≈ −0.2° (elevator only +2.2–2.4°), ~2.5–2.7° below the true level-flight attitude; with no TECS that shortfall is the entire sink (reducing throttle makes it *worse*). Zero actuator clamp events any layer; `roll_mav≈+roll_gz`, `pitch_mav≈−pitch_gz` hold throughout. **Part E outcome: (B) `ARDUPLANE_FBWA_CONTROL_REFERENCE_MISMATCH`** — the physical airframe+aero+propulsion flies level at the reference command point (confirmed by two fully independent paths: pure Gazebo, and ArduPlane MANUAL which keeps the full bridge/lockstep/frame layer); the bridge/transport/lockstep/frame layer is NOT the cause; FBWA's neutral-stick 0° pitch reference is inconsistent with the true +2.5° nose-up level-flight attitude. **Routed to `controls-integration`** (a deliberate, documented ArduPlane parameter change — a small nose-up level-pitch reference `PTCH_TRIM_DEG`/`TRIM_PITCH_CD` ≈ +2.5° and/or speed-coupled-throttle/TECS enable — captured in `docs/source_of_truth/controls/` + `falcon_v2_sitl.parm`) **and `validation`** (independent review; force-balance write-up and the final stage report are the next steps, deliberately not written by this pass). No aircraft physics parameter modified anywhere in this pass; **no** aero/propulsion/mass/CG/authority change is indicated — the airframe already flies level. New scripts: `tests/gazebo/scripts/test_ardupilot_longitudinal_equilibrium_root_cause.py`, `tests/gazebo/scripts/test_ardupilot_longitudinal_equilibrium_fbwa_crosscheck.py`, `tests/gazebo/scripts/run_ardupilot_longitudinal_equilibrium_c3.sh`. Raw results: `tests/gazebo/results/ardupilot_longitudinal_equilibrium_{c1_reference,sweep,c3_fbwa_throttlestar,c3_fbwa_original,c3_manual}_result.json` (+ `_log.txt` for c1/sweep).

**Update, 2026-08-29 (`ARDUPLANE_FBWA_LEVEL_PITCH_REFERENCE_CORRECTION` — Step 3, `gazebo-testing`):** SHORT bounded verification of `controls-integration`'s applied fix — `PTCH_TRIM_DEG 2.49` added to `config/ardupilot/falcon_v2_sitl.parm` (was default 0.0; a pure FBWA/attitude-controller demand offset per `ArduPlane/Attitude.cpp:244`, verified V4.8.0-dev commit 409226a637 — does not touch AHRS/EKF, does not affect MANUAL) plus the canonical trim constants in `test_ardupilot_basic_closed_loop_flight.py` (`base`) corrected to the proven pure-Gazebo equilibrium C.2 (`V_TRIM 18.162`, `ALPHA_TRIM_DEG 2.472`, `ELEVATOR_THETA 4.092°`, `TRIM_THROTTLE 0.4957` → `RC3_TRIM_TARGET_US` resolves to **1496**). No aero coefficient/table, propulsion parameter, PID gain, actuator/sign mapping, joint limit, plugin source or SDF was modified — new test script + raw capture only. **ONE neutral-stick FBWA segment, 28 s, 5 s transient cutoff, ~20 Hz combined MAVLink + gz-transport telemetry** (RC1=RC2=1500, RC3=1496, RC4=1500, RC5=1000), reusing `base` PHASE 1–4 precondition and `campaign` (`enter_fbwa`/`run_segment`/`build_sample`) **verbatim** — exact pattern of `test_ardupilot_trim_reference_correction_validation.py`. Fresh `gz sim` + gdb-wrapped `arduplane` pair, world `falcon_v2_ardupilot_basic_closed_loop_flight_world.sdf`. Runtime PARAM_REQUEST_READ confirmed `PTCH_TRIM_DEG = 2.49`, `KFF_THR2PTCH = 0`, `FLIGHT_OPTIONS = 0`. **PITCH TELEMETRY CAVEAT handled:** `GCS_MAVLink_Plane.cpp:139` subtracts `PTCH_TRIM_DEG` from reported `ATTITUDE.pitch`, so with the fix working the mav pitch reads ≈ 0° while the aircraft is physically +2.49° nose-up — the acceptance signal is read from gz ground-truth pose (`−pitch_gz`, nose-down-positive in this FLU world → physical nose-up = `−pitch_gz`). **Result (stabilized window t ≥ 5 s, n = 438; last-5 s "settled" figures in parens):** `NAV_CONTROLLER_OUTPUT.nav_pitch` demand **0.000°** (all samples); physical pitch nose-up **+2.02° (+2.34°)** mean, std 0.30°, converging monotonically toward +2.49° with q ≈ 0, no oscillation growth; `ATTITUDE.pitch` mav **−0.47°** and the new invariant **`pitch_mav ≈ pitch_phys − 2.49` holds to max\|resid\| 0.008°**; airspeed **18.43 m/s (18.20 m/s)**, slope −0.033 m/s² (converging to the 18.16 trim, NOT diverging to ~19.4 like the prior FBWA runs); **vertical speed −0.078 m/s by all three methods** (pos_z linear regression, endpoint average, odometry twist-z world-projected — agree to <0.001 m/s; VFR_HUD.climb −0.0785 m/s), **−0.029 m/s over the settled last 5 s and still shrinking**; altitude slope −0.078 m/s; throttle actual L/R **0.494 / 0.495** (asym 0.001, within 0.01 of 0.4957); thrust 4.73 N, drag q̄·S·CD 5.20 N, **T − D = −0.48 N** (was −1.76 / −1.94 N); L/W 0.997; elevator cmd≈actual **+3.66° (+3.96°)** both halves, tracking to <0.01°, converging toward the +4.09° trim; **zero actuator target/effort clamp events any surface**; roll/yaw/p/r at noise floor (`roll_mav ≈ +roll_gz`, max diff 0.009°); **zero NaN/Inf**; FBWA (`custom_mode 5`) all samples; oscillation-growth check — all three signals (altitude, airspeed, pitch) damping OUT (2nd-half residual std 0.53–0.86× the 1st half). No `gz`/`arduplane` crash. **Side-by-side vs the prior stage's FBWA runs (same world / same precondition, only `PTCH_TRIM_DEG` + constants differ): steady sink −0.551 m/s (FBWA-original) / −0.598 m/s (FBWA-throttle\*) → −0.078 m/s here (~86 % reduction, −0.029 m/s settled)**; physical pitch −0.2° → +2.0–2.3°; airspeed 19.4–19.5 → 18.2–18.4 m/s; T−D −1.8 N → −0.5 N. **VERDICT: `FBWA_LEVEL_PITCH_REFERENCE_CORRECTION_PASS`** — `|vertical_speed| ≤ 0.10 m/s`, physical pitch holds ≈ the true +2.49° nose-up level attitude, airspeed converges to trim, zero clamp, throttle ≈ 0.4957, no growing oscillation. Residual 0.03–0.08 m/s sink (for the record, not a defect, NOT tuned here): the 28 s window is shorter than a full ~9 s phugoid (ζ≈0.22) ring-down, and a genuine sub-threshold ~0.5 N fixed-throttle thrust deficit persists at the slightly-above-trim airspeed still being settled through — the same no-TECS energy mechanism from the root-cause report §15.2, now an order of magnitude smaller; a true mathematical zero would be the separate TECS / speed-coupled-throttle item. Two first-pass acceptance sub-checks that false-negatived on the converging (not diverging) trajectory were replaced with convergence-aware criteria + an explicit last-5 s endpoint-window check, and the captured raw data re-analysed offline (`--reanalyze`, flight NOT re-flown); no threshold was loosened and no aircraft-physics parameter was touched. New script: `tests/gazebo/scripts/test_ardupilot_fbwa_level_pitch_reference_correction.py`; launch helper: `tests/gazebo/scripts/run_ardupilot_fbwa_level_pitch_reference_correction.sh`. Raw: `tests/gazebo/results/ardupilot_fbwa_level_pitch_reference_correction_{result.json,log.txt}` (all raw timeseries kept). **Routed to `validation`** for independent review of the parameter change, the semantics claims, and this test result.

**Update, 2026-09-02 (`ARDUPLANE_TECS_AND_CRUISE_SPEED_HOLD_VALIDATION` — BASELINE run, `gazebo-testing`):** Executed `controls-integration`'s delivered FBWB/TECS campaign **exactly as written, once, unmodified** (`./tests/gazebo/scripts/run_ardupilot_tecs_cruise_speed_hold.sh`). **No `TECS_*`, `TRIM_THROTTLE`, PID, aero, propulsion, actuator, sensor, SDF, plugin, trim constant or acceptance threshold was changed** — `git diff` empty, no re-fly, no `--reanalyze`. Mode **FBWB (`custom_mode 6`)**: TECS drives BOTH throttle (airspeed) and pitch (height); no navigation controller. TECS on ArduPlane V4.8.0-dev (409226a6) **firmware defaults** (live dump: `TECS_CLMB_MAX 5.0`, `TECS_SINK_MIN 2.0`, `TECS_SINK_MAX 5.0`, `TECS_TIME_CONST 5.0`, `TECS_THR_DAMP 0.5`, `TECS_PTCH_DAMP 0.3`, `TECS_INTEG_GAIN 0.3`, `TECS_SPDWEIGHT 1.0`, `TECS_HDEM_TCONST 3.0`; `THR_MIN 0`/`THR_MAX 100`, `TRIM_THROTTLE 45`, `FBWB_CLIMB_RATE 2.0`, `PTCH_TRIM_DEG 2.49`); **all 16 param preconditions true**, 1367 params bulk-read, none missing. Segments A level cruise 45 s → B +10 m climb ramp + 35 s hold → C −10 m descent ramp + 30 s hold; **flight 127.47 s, no abort, 2444 samples @ 20 Hz**. **VERDICT: `TECS_CRUISE_SPEED_HOLD_PASS` — 43/43 acceptance criteria PASS, `failed_checks = []`**, including the non-gating preferred tight altitude bound. **TECS authority proven:** RC3 1258 µs would give 0.1975 throttle as manual passthrough; measured **0.4911** → |Δ| **0.2936**. **Speed hold:** A_hold airspeed **17.926 ± 0.187 m/s**, slope +0.000088 m/s², **0.005 m/s from TECS's own demand** (dataflash `TECS.spdem − sp` = −0.0013 m/s TAS); whole-flight 16.780…19.342 m/s, never near `AIRSPEED_MIN` 16 or the 14.4 underspeed trigger. **Altitude hold:** vz **+0.00103 / +0.00109 / +0.00829 m/s** (A/B/C hold), p2p 1.30 / 2.18 / 0.97 m — **the FBWA 0.078 m/s residual sink is CLOSED (76× better, and it is a negligible climb not a sink)**; C_hold recaptured the original altitude to −0.074 m after the full ±10 m round trip. **Step:** +10.239 m in 8.670 s (vz +1.301 m/s reg, +1.951 m/s peak vs the 2.0 m/s demand) and −10.314 m in 8.845 s (−1.301 / −1.847 m/s); **both gating direction checks PASS** — `FBWB_ELEV_REV 0` confirmed by measurement, not assumed. **Throttle:** 0.4911 / 0.4907 / 0.4909 in the three hold windows vs measured trim* 0.4957 (−0.005, expected at the 17.93 m/s hold); climb 0.5398, descent 0.4251; **0.0 s at THR_MIN and 0.0 s at THR_MAX**; L/R asymmetry ≤ 0.001. **Pitch:** level physical +2.663° (vs `PTCH_TRIM_DEG` 2.49), demand +2.713°, elevator **+4.417°** vs the +4.092° pure-Gazebo trim reference (correct direction for the 0.24 m/s slower cruise); `pitch − (alpha + gamma)` residual **+0.0017° mean, max |·| 0.118°**; all four pitch/throttle coordination sign checks PASS (climb +0.049 throttle / +4.02° pitch; descent −0.066 / −3.92°). **Force closure:** T − D = **+0.038 N**, L/W **0.9988** in A_hold (FBWA stage was −0.48 N). **Motors:** RPM 2263.7…6879.1, thrust 0…6.000 N per motor, max |L−R| 10.5 RPM / 0.0335 N. **Saturation/numerics:** elevator max |6.368°| and lateral max |0.505°| of ±45° travel, **zero actuator target/effort clamp samples**, **zero NaN/Inf**, all oscillation-growth ratios **0.41–0.65 (damping out)** in every window/channel, FBWB on every sample, zero wind confirmed (|GS−AS| ≤ 0.196 m/s, `SIM_WIND_SPD 0`). **Findings that do NOT change the verdict and were NOT 'fixed' here:** (1) the nominal "18.000 m/s target" is not exactly reachable — `RC_Channel::get_control_in()` returns `int16_t` (`RC_Channel.h:99/542`), so control_in 16.623 truncates to 16 and `navigation.cpp:187-189` yields `target_airspeed_cm = 1792` = **17.92 m/s**; the −0.074 m/s offset from 18.000 is therefore command quantization, not controller error (TECS tracked its demand to 0.005 m/s) — **owner `controls-integration`**; (2) the APC 13x6.5E Ct/Cp lookup reported **`interpClamped` on 176/4888 motor-samples (3.6 %)** at J 0.644–1.457 during spin-up and the low-throttle descent, floor-clamping thrust to **exactly 0 N on 84 samples** where a real fixed-pitch prop would windmill with negative thrust — a source-table domain limit, **owner `propulsion`**, currently covered by no acceptance criterion; (3) dataflash `ERR Subsys 24 (EKF_PRIMARY)` lane switch 0→1→0 at flight t ≈ 3.3–9.1 s, entirely inside the 12 s Segment A transient cutoff, no failsafe/mode change; (4) `..._gz_log.txt` is 0 bytes = **zero Gazebo errors/warnings**; `arduplane` log clean, no gdb backtrace (no crash), `PM` records `NLon 0 / ErrL 0 / InE 0`. Artifacts: `tests/gazebo/results/ardupilot_tecs_cruise_speed_hold_{result.json,timeseries.json (7.58 MB, 20 Hz, complete),log.txt,gz_log.txt,arduplane_log.txt}` and `..._dataflash/00000001.BIN` (7.32 MB, carries the `TECS` message with internal SPE/SKE-side state not exposed over MAVLink). Full writeup: `docs/test_results/2026-09-02_ardupilot_tecs_and_cruise_speed_hold_validation.md`. **Routed to `validation`** for independent review.

**Update, 2026-09-02/03 (`SITL_ATMOSPHERE_AND_PITOT_INTEGRATION_VALIDATION` — airspeed/wind acceptance matrix + short TECS regression + Task-C housekeeping, `gazebo-testing`):** Acceptance test of `controls-integration`'s two fixes (record: `docs/source_of_truth/autopilot/SITL_ATMOSPHERE_AND_AIRSPEED.md`) — **M-1** drop `-O 0,0,0,0` (which `SITL_cmdline.cpp:761-766` silently rewrote to the 584 m CMAC elevation) in favour of `SIM_OPOS_*`, and **M-2** `ArduPilotPlugin` now emits the official SIM_JSON `airspeed` (EAS, from `FalconV2Pitot`) and `velocity_wind` (NED, from `FalconV2Wind`) keys so `SIM_JSON.cpp:445`'s `wind_ef.zero()` else-branch is no longer taken. **No aero coefficient, propulsion value, PID, `TECS_*`, `PTCH_TRIM_DEG`, control-surface scaling, actuator parameter, mass/CG/inertia, `.parm`, SDF or plugin was modified by this pass**; the only tracked edits are the two Task-C runner scripts. `SIM_ARSPD_RND 0` was set over MAVLink **per test only** (never written to `falcon_v2_sitl.parm`) and restored afterwards, because the firmware default 2.0 rectifies through `sqrt(|ratio*(q+noise)|)` (`sitl_airspeed.cpp:37-39`) into a positive near-zero-airspeed bias.

**Design.** Four independent runs (fresh `gz sim` + gdb-`arduplane` pair each, wiped EEPROM, so no EKF/TECS integrator state carries across): `zero`, `headwind` (world-ENU airmass velocity `-5,0,0`), `tailwind` (`+5,0,0`), plus a brief `crosswind` (`0,+5,0`) frame sanity case. **No test world was changed** — `FalconV2Wind` is a model-level plugin present in every world, defaulting to zero and commandable live on `/model/falcon_v2/wind/steady_cmd`. Every run launches at **zero** wind and flies an identical `P0` 18 m/s FBWB reference cruise before the wind is switched on, so the cases start from a common flight condition. The stage's central measurement problem is stated and handled explicitly rather than glossed: in closed-loop TECS cruise **TECS holds AIRSPEED, so it is the GROUNDSPEED that shifts by 5 m/s** — the same physics as "airspeed shifts by 5 at fixed groundspeed", but the two are never converted into one another. Both are therefore measured separately: **(P1) matched-groundspeed steady segments** and **(P2) closed-loop cruise**. A single matched groundspeed serving both wind signs is **impossible** for this airframe (it would need `G ≤ ~16.5` and `G ≥ ~21` at once, given `AIRSPEED_MIN 16` and `NO_VALID_TRIM` at ≥ 24 m/s per `flight_envelope_result.json`), so the matched-groundspeed comparison is done **pairwise at two groundspeeds** — a recorded envelope limitation, not a fudge. Demands are derived at runtime from live parameters **including the `int16_t control_in` truncation** (`RC_Channel.cpp:316`), giving the reachable 16.00 / 17.92 / 20.92 m/s rather than the nominal 16 / 18 / 21.

**Result: `AIRSPEED_WIND_ACCEPTANCE_MATRIX_PASS` — 29/29 cross-case checks and 4/4 per-case runs PASS, `failed_checks = []`.** **Matched groundspeed, headwind** (zero@16.00 vs headwind@20.92, groundspeeds **16.030 → 16.028**, mismatch **0.001 m/s**): airspeed **16.030 → 20.941**, Δ **+4.911 m/s**; ArduPlane `ARSP.Airspeed` Δ **+4.917**; mismatch-corrected identity `ΔV_air − ΔV_ground` = **+4.913** (expected +5.000, err −0.087); `TECS.sp` Δ **+4.945 TAS**. **Matched groundspeed, tailwind** (zero@20.92 vs tailwind@16.00, groundspeeds **20.916 → 21.003**, mismatch 0.087 m/s): airspeed **20.918 → 16.016**, Δ **−4.902**; ArduPlane Δ **−4.955**; identity **−4.989** (err +0.011); `TECS.sp` Δ **−4.975 TAS**. **Closed-loop 18 m/s cruise**: airspeed **17.941 / 17.941 / 17.936** for zero/head/tail — spread **0.0047 m/s** across all three — while groundspeed goes **17.941 / 12.944 / 22.928**, i.e. shifts of **−4.995 / +5.001 m/s** against the commanded ±5 (errors 0.005 / 0.001 m/s). **ArduPlane vs Gazebo pitot: worst |ARSP − pitot| = 0.033 m/s** over all 12 windows (live `VFR_HUD` − pitot: worst 0.025 m/s). **Gazebo pitot vs analytic ground truth** `|V_ground_world − V_wind_world|`: mean error **−0.0001 … +0.0018 m/s**. **`EAS2TAS` (dataflash `TECS.sp / ARSP.Airspeed`) = 1.0032 … 1.0047** across every window — the post-M-1 sea-level datum, vs **1.0334** pre-fix; `ORGN.Alt = 0.0 m` and `BARO.Press ≈ 100 37x Pa` in all four runs. **`ARSP` sensor used and healthy on 100 % of in-flight samples** (`ARSPD_OPTIONS = 11` does **not** disable it in flight). **Frame sanity — `ArduPlane`'s own AHRS wind estimate** (MAVLink `WIND` ← `AP_AHRS_SIM.cpp:168` ← the `velocity_wind` key; **identically zero before M-2**) reads **5.000 m/s @ 90.0° / −90.0° / 180.0°** for the head/tail/cross cases, i.e. FROM East / FROM West / FROM South — **exactly** the ENU→NED mapping the commanded `(-5,0,0)/(+5,0,0)/(0,+5,0)` airmass velocities imply, bearing error **0.00°**; the crosswind case is what rules out an X/Y swap the along-X cases cannot see. **Numerics/saturation, all 12 windows: zero NaN/Inf, 0.00 s at `THR_MIN` and 0.00 s at `THR_MAX`, zero actuator target/effort clamp samples, `gz` logs 0 bytes (no Gazebo error/warning).**

**Honest caveat — sustained phugoid at the 20.92 m/s demand (NOT a wind/transport defect, NOT tuned here).** Both `P1_MG_HIGH` segments show a large, only marginally damped longitudinal oscillation after a 25 s settle: airspeed std **2.14 m/s** (zero wind) / **1.97** (headwind), physical pitch **−13.3…+15.4°**, altitude p2p **9.9 / 8.4 m**, throttle **0.34–0.71**, second-half/first-half std ratio **0.93 / 0.97** — bounded and slowly decaying, but not a hold. It is present at **zero wind** at the same demand, which is the control that proves it is a property of the ~21 m/s flight condition (near the top of the trimmable envelope; `flight_envelope_result.json` classes 21.0 `MARGINAL_TRIM` and 24.0 `NO_VALID_TRIM`), not of the airspeed/wind path. The 16.00 and 17.92 m/s demands are by contrast rock-solid (airspeed std 0.015–0.20 m/s, altitude p2p 0.13–2.8 m). The matched-groundspeed comparison is unaffected — it averages 3–4 oscillation periods and the mismatch-corrected identity form is insensitive to it — but the oscillation is **reported, not smoothed over**, and is **routed to `controls-integration`** as a TECS characterization item for the eventual `TECS_CLIMB_SINK_PERFORMANCE_CHARACTERIZATION` stage. Also recorded: `phase2_teleport_and_verify` remains flaky (≈ 30–40 % first-try success; `zero` needed 5 attempts, `crosswind` 7) — a pre-existing harness/transport issue; **its acceptance gate was NOT loosened**, runs were simply repeated.

**Task-C housekeeping (all `gazebo-testing`-owned).** (1) `-O 0,0,0,0` **removed** from `run_ardupilot_longitudinal_equilibrium_c3.sh` and `run_ardupilot_fbwa_level_pitch_reference_correction.sh` — **fixed rather than retired**, because results from both are still cited elsewhere; each now carries an explicit warning that a re-run will differ from its archived pre-fix numbers by roughly the EAS2TAS ratio change (−2.8 % on TAS-derived quantities), which is a correction, not a regression. (2) New param precondition **`sim_opos_alt_zero_atmosphere_datum` (`SIM_OPOS_ALT == 0`)** added to `test_ardupilot_tecs_cruise_speed_hold.py` and asserted by every run of the new acceptance test, so the datum cannot silently regress; it fails on a missing parameter too. (3) `validation` **MINOR-2/3/4 closed**: the command inversion now models the `int16_t` truncation and reports the true reachable demand (**17.92**, matching the dataflash-derived 17.9248) instead of the optimistic 18.000 — **the transmitted PWM is deliberately unchanged at 1258 µs**, since moving it to the next 0.12 m/s grid point would change the commanded flight condition and hence the validated baseline; the three inline literals in `verdict()` are named (`TH_SPEED_SLOPE_MAX_MS_PER_S`, `TH_ALT_STEP_ACHIEVED_FRAC`, `TH_RAMP_DIRECTION_MIN_MS`) with values **unchanged** and now recorded in the result JSON's `thresholds` block; the vacuous-pass paths (`x is None or …`, and B/C checks that only existed when their window did) now **fail** on missing data. Non-perturbation was verified by `--reanalyze` against the validated afterfix timeseries: every physics check reproduces identically, the only delta being the new datum guard, which the older capture cannot satisfy because `SIM_OPOS_ALT` was not in its parameter dump (`ardupilot_tecs_cruise_speed_hold_afterfix_reanalysis_minorfix_result.json`). (4) One **test-logic bug found and fixed in this stage's own analyzer**, self-reported: the first dataflash time-alignment implementation searched a fixed [−5, +25] s offset window and railed at +25 s in every case (the true offset is the 43–60 s FBWB-entry boot time), producing garbage `ARSP`/`TECS` columns and a physically impossible `EAS2TAS = 0.989`; the search is now centred on the `MODE→FBWB` event, aligned on **groundspeed** (deliberately not airspeed, so it cannot manufacture ARSP-vs-pitot agreement), and **refuses to emit columns at all** if it rails or the residual RMS exceeds 0.5 m/s — achieved residual **0.022–0.249 m/s**, implied flight-t0-minus-mode-entry **0.00–0.10 s**. `EAS2TAS` is now the skew-robust ratio of window means rather than a pointwise ratio. New scripts: `tests/gazebo/scripts/test_ardupilot_airspeed_wind_acceptance.py`, `run_ardupilot_airspeed_wind_acceptance.sh`, `build_airspeed_wind_matrix.py`. Artifacts: `tests/gazebo/results/airspeed_wind_acceptance_{zero,headwind,tailwind,crosswind}_{result.json,timeseries.json,log.txt,gz_log.txt,arduplane_log.txt}` + `..._dataflash/*.BIN`, and the assembled `airspeed_wind_acceptance_matrix.{json,txt}`. **Routed to `validation`** for independent review.


## Directory layout

```
tests/
  gazebo/       # Gazebo Sim Harmonic world / model-load / runtime tests (this directory)
  physics/      # lower-level physics checks (gravity, inertia, numerical stability)
  regression/   # regression suite run after any implementation change
```

Test results are saved to `docs/test_results/`.

## Planned test scenarios

Owned by `gazebo-testing`, independently reviewed by `validation`. **None are implemented yet — this is registration/documentation only, no test was run to produce this table.** Each row cites the specific source-of-truth document/section the test will check its result against once an implementation exists; `gazebo-testing` must never adjust an aircraft physics parameter to make any of these pass (`CLAUDE.md` hard constraint).

### 1. Foundational / load / static

| Test | Validates | Source-of-truth citation |
|---|---|---|
| `MODEL_LOAD_TEST` | FALCON V2 SDF loads into Gazebo Sim Harmonic with no parse/link/joint/mesh-resolution errors. | `docs/source_of_truth/geometry/GEOMETRY.md` §4.2 (12-mesh inventory), §26.3 (movable-link/fixed-structure split); no SDF exists yet — this is a load-time structural check, not a coefficient check. |
| `GROUND_NO_WIND_TEST` | At rest on the ground, throttle=0, zero wind: no spurious motion, no NaN/divergence, model settles under gravity. | Master dataset §68 item 1 ("Ground/no-wind, throttle=0"). |
| `MASS_CG_INERTIA_TEST` (folds prior `STATIC_GRAVITY_TEST` + `CG_BALANCE_TEST`) | SDF `<inertial>` mass=6.000 kg, CG=(0.168309, 0, 0.100000) m, and the V1 inertia tensor produce the expected static balance and free-rotation response (no unexpected pitch/roll/yaw drift at rest). | `docs/source_of_truth/mass_properties/MASS_PROPERTIES.md` §1, §2, §3.1, §3.4 (CG), §5.1 (`V1_PROVISIONAL` inertia tensor, incl. the flagged Ixz-sign-vs-SDF prerequisite). |

### 2. Control-surface sign / direction

| Test | Validates | Source-of-truth citation |
|---|---|---|
| `CONTROL_SURFACE_DIRECTION_TEST` (umbrella) | Each of the 5 control-surface joints and 2 prop joints, commanded in isolation, produces the expected physical motion before any downstream aero/prop force is trusted. General cross-check preceding `ROLL_RESPONSE_TEST`/`PITCH_RESPONSE_TEST`/`YAW_RESPONSE_TEST`. | `docs/source_of_truth/controls/CONTROLS.md` §4 (governing rule: "XFLR5 control sign ile Gazebo joint sign aynı varsayılmamalı"), §4.4. |
| `AILERON_TEST` | Resolves `AILERON_SIGN_TEST_REQUIRED` (left and right independently) — commanded left/right aileron deflection produces the correct physical up/down direction and roll-moment sign consistent with `Clδa≈+0.308/rad`. | `CONTROLS.md` §4.3, §7 item 4; `AERODYNAMICS.md` §7.3, §10. |
| `ELEVATOR_TEST` | Resolves `ELEVATOR_SIGN_TEST_REQUIRED` — commanded elevator deflection produces the correct TE-down(+)/TE-up(−) direction (XFLR5 convention) and pitching-moment sign consistent with `Cmδe≈-0.73/rad`. | `CONTROLS.md` §4.1, §7 item 4; `AERODYNAMICS.md` §7.1, §10. |
| `RUDDER_TEST` | Resolves `RUDDER_SIGN_TEST_REQUIRED` — commanded rudder deflection produces the correct physical left/right direction (not stated by XFLR5 itself) and yaw-moment sign consistent with `Cnδr≈-0.025/rad`. | `CONTROLS.md` §4.2, §7 item 4; `AERODYNAMICS.md` §7.2, §10. |

### 3. Propulsion

| Test | Validates | Source-of-truth citation |
|---|---|---|
| `PROP_SPINUP_TEST` | Throttle step produces RPM rising as a solved state through `I_rotor·dω/dt = Q_motor − Q_prop` — never set instantaneously. | Master dataset §68 item 2; `docs/source_of_truth/propulsion/PROPULSION.md` §2, §4. |
| `STATIC_THRUST_RPM_TEST` | At zero airspeed, per-motor thrust-vs-RPM falls in the validated reference band (SunnySky bench ≈9230 RPM→≈32.85 N; APC-official interpolation ≈31.32 N at the same RPM). | Master dataset §68 item 3; `PROPULSION.md` §1.6. |
| `STATIC_POWER_CURRENT_TEST` | Static current (≈63.2 A) and power (≈935 W/motor) plausibility at the bench reference RPM. | Master dataset §68 item 4; `PROPULSION.md` §1.6. |
| `LEFT_MOTOR_TEST` / `RIGHT_MOTOR_TEST` | Each motor's RPM/thrust/torque is computed independently at its own hub and local airspeed, not lumped into one combined value. | `PROPULSION.md` §6 ("each motor's thrust and torque must be computed independently"). |
| `REACTION_TORQUE_TEST` | Propeller shaft reaction torque `Q_reaction = −Q_prop` is applied to the airframe at each real hub location; sign verified against Gazebo FLU convention. | Master dataset §68 item 5, §72 ("reaction torque mutlaka uygulanmalı"); `PROPULSION.md` §7, §7.2 (pitch-moment-sign flag). |
| `COUNTER_ROTATION_CANCELLATION_TEST` (folds prior `SYMMETRIC_PROPULSION_TEST`) | Equal throttle on both motors (left CCW / right CW) yields a net reaction moment ≈0 at the airframe. | Master dataset §68 item 6; `PROPULSION.md` §1.4, §7. |
| `DIFFERENTIAL_THRUST_YAW_TEST` | Asymmetric throttle produces `Mz ≈ 0.300 × (T_right − T_left)` arising from real `r×F` at the hub positions — no coded yaw-control derivative. | Master dataset §68 item 7; `PROPULSION.md` §7.1. |
| `ENGINE_OUT_TEST` | Stopping one motor produces the expected residual yaw/roll moment and continued (non-diverging) flight response. | Master dataset §68 item 8. |

### 4. Aerodynamic sign & zero-airspeed behavior

| Test | Validates | Source-of-truth citation |
|---|---|---|
| `ZERO_AIRSPEED_AERO_TEST` | At V=0, the aerodynamic force/moment model does not diverge/NaN (`qbar=0`, `J`/advance-ratio edge cases handled). | `docs/source_of_truth/aerodynamics/AERODYNAMICS.md` §8.1, §8.4. |
| `AOA_SIGN_TEST` | `alpha = atan2(w,u)` produces the correct sign for FALCON V2's FLU body frame (flagged risk: the stated formula is a standard FRD form; FLU flips the Z axis relative to FRD). | `AERODYNAMICS.md` §8.1, §10, §13 item 10. |
| `SIDESLIP_SIGN_TEST` | `beta = asin(v/V)` produces the correct sign for FLU (FLU flips the Y axis relative to FRD). | `AERODYNAMICS.md` §8.1, §10, §13 item 10. |

### 5. Trim & straight-level flight

| Test | Validates | Source-of-truth citation |
|---|---|---|
| `TRIM_TEST` (umbrella) | The implemented aircraft can find and hold *a* trimmed condition at all, before checking it against a specific numeric benchmark. | `AERODYNAMICS.md` §6.7, §6.8 (`VALIDATION_TARGET`s). |
| `18MPS_LEVEL_TRIM_TEST` (folds master-dataset benchmark items 9–10) | Trim at V≈18 m/s: `CLreq≈0.657`, total drag/required thrust≈5.19 N (≈2.595 N/motor), elevator≈−8°. | Master dataset §68 items 9–10, §69; `AERODYNAMICS.md` §6.7, §6.8. |
| `NEUTRAL_ELEVATOR_21P24_TEST` | Trim at neutral (0°) elevator: V≈21.244 m/s, alpha≈0.364°, CL≈0.4717 — the project's original single full-aircraft reference point. | Master dataset §68 item 11, §69; `AERODYNAMICS.md` §6.1, §6.8; `CLAUDE.md`. |
| `STRAIGHT_LEVEL_FLIGHT_TEST` | Sustained straight-and-level flight *holds* trim over time (dynamic persistence), not just a one-shot static solve. | `AERODYNAMICS.md` §6.7 (`VALIDATION_TARGET`, "primary future straight-and-level Gazebo benchmark"). |

### 6. Control response (rate-domain)

| Test | Validates | Source-of-truth citation |
|---|---|---|
| `AILERON_ROLL_TEST` | Commanded aileron deflection produces a roll-rate response consistent with `Clδa`/`Clp`, run only after `AILERON_TEST` confirms direction. | Master dataset §68 item 12; `AERODYNAMICS.md` §6.2, §7.3. |
| `RUDDER_YAW_TEST` | Commanded rudder deflection produces a yaw response consistent with `Cnδr`/`Cnr`/`Cnβ`, run only after `RUDDER_TEST` confirms direction. | Master dataset §68 item 13; `AERODYNAMICS.md` §7.2. |
| `ROLL_RESPONSE_TEST` | Full roll-rate step/settle response cross-checked against the roll-subsidence mode (λ≈−9.464 1/s, τ≈0.106 s). | `AERODYNAMICS.md` §6.4, §6.8; `CONTROLS.md` §4.4. |
| `PITCH_RESPONSE_TEST` | Elevator-step → pitch-rate response cross-checked against `Cmq` and the short-period mode. | `AERODYNAMICS.md` §6.2, §6.4. |
| `YAW_RESPONSE_TEST` | Rudder/differential-thrust step → yaw-rate response cross-checked against `Cnr` and the Dutch-roll mode. | `AERODYNAMICS.md` §6.4; `CONTROLS.md` §4.4. |

### 7. Dynamic modes, stall approach, manufacturer comparison

| Test | Validates | Source-of-truth citation |
|---|---|---|
| `DUTCH_ROLL_TEST` | Lateral disturbance excites Dutch roll at fn≈0.512 Hz, ζ≈0.095, period≈1.96 s. | Master dataset §68 item 14, §69; `AERODYNAMICS.md` §6.4, §6.8. |
| `ROLL_SUBSIDENCE_TEST` | Roll mode λ≈−9.464 1/s, τ≈0.106 s. | Master dataset §68 item 15, §69; `AERODYNAMICS.md` §6.4, §6.8. |
| `SPIRAL_MODE_TEST` | Mildly unstable spiral mode λ≈+0.08227 1/s, doubling time≈8.43 s — expected/reported behavior, **not a defect to "fix" by adjusting a derivative**. | Master dataset §68 item 16, §69; `AERODYNAMICS.md` §6.4 (explicit non-tuning instruction). |
| `SHORT_PERIOD_TEST` | Short-period mode ζ≈0.394. | Master dataset §68 item 17, §69; `AERODYNAMICS.md` §6.4, §6.8. |
| `PHUGOID_TEST` | Phugoid mode ζ≈0.003 (stable but very lightly damped). | Master dataset §68 item 18, §69; `AERODYNAMICS.md` §6.4, §6.8. |
| `STALL_APPROACH_TEST` | Behavior approaching stall (alpha→≈9–9.5°, V→≈Vstall≈12.24 m/s) is characterized against the documented V1 linear-model reliability limit — **not** validated against any stall/post-stall model, since none is chosen yet (explicitly deferred, `AERODYNAMICS.md` §11). | Master dataset §68 item 19; `AERODYNAMICS.md` §5.2, §11. |
| `MANUFACTURER_6KG_CURVE_COMPARISON` | Simulated level-flight drag/thrust/RPM/throttle vs. airspeed (12.5–25 m/s) compared against the `V1_CALIBRATED`/`V1_VALIDATION_ESTIMATES` tables. | Master dataset §68 item 20, §69; `AERODYNAMICS.md` §6.6; `PROPULSION.md` §6.1. |

### 8. Numerical stability (cross-cutting)

| Test | Validates | Source-of-truth citation |
|---|---|---|
| `NUMERICAL_STABILITY_TEST` | No NaN/divergence/energy-gain artifact across the integration timestep, for any of the scenarios above (ground, spin-up, trim, dynamic-mode excitation). | `CLAUDE.md` simulation-tuning-policy checklist items 11–12 (integration timestep, numerical stability). |

## Reconciliation with the prior 19-item list

The prior planned-test list (unchanged, still recorded verbatim in `docs/architecture/AGENT_WORKFLOW.md` "Future test categories") is not duplicated above — it is folded in as follows, so nothing is silently dropped:

| Prior list item | Disposition in this pass |
|---|---|
| `MODEL_LOAD_TEST` | Kept as-is (§1). |
| `STATIC_GRAVITY_TEST`, `CG_BALANCE_TEST` | Folded into `MASS_CG_INERTIA_TEST` (§1) — same underlying check (mass/CG/inertia static behavior), one combined name. |
| `CONTROL_SURFACE_DIRECTION_TEST` | Kept as the umbrella for `AILERON_TEST`/`ELEVATOR_TEST`/`RUDDER_TEST` (§2). |
| `AILERON_TEST`, `ELEVATOR_TEST`, `RUDDER_TEST` | Kept, now explicitly tied to the `AILERON_SIGN_TEST_REQUIRED`/`ELEVATOR_SIGN_TEST_REQUIRED`/`RUDDER_SIGN_TEST_REQUIRED` tags from `CONTROLS.md` §4 (§2). |
| `LEFT_MOTOR_TEST`, `RIGHT_MOTOR_TEST` | Kept, now cited against `PROPULSION.md` §6's per-motor-independence rule (§3). |
| `SYMMETRIC_PROPULSION_TEST` | Folded into `COUNTER_ROTATION_CANCELLATION_TEST` (§3) — same check (equal-throttle reaction-torque cancellation), master-dataset-aligned name. |
| `ZERO_AIRSPEED_AERO_TEST` | Kept as-is (§4). |
| `AOA_SIGN_TEST`, `SIDESLIP_SIGN_TEST` | Kept as-is, now cited against the explicit FRD-vs-FLU risk flagged in `AERODYNAMICS.md` §8.1/§10 (§4). |
| `TRIM_TEST` | Kept as the umbrella over the two concrete numeric benchmarks `18MPS_LEVEL_TRIM_TEST` and `NEUTRAL_ELEVATOR_21P24_TEST` (§5). |
| `STRAIGHT_LEVEL_FLIGHT_TEST` | Kept, distinguished from `18MPS_LEVEL_TRIM_TEST` as the dynamic-persistence check vs. the static-benchmark-match check (§5). |
| `ROLL_RESPONSE_TEST`, `PITCH_RESPONSE_TEST`, `YAW_RESPONSE_TEST` | Kept as-is, now explicitly sequenced after the corresponding sign test (§6). |
| `ENGINE_OUT_TEST` | Kept as-is (§3), matches master dataset §68 item 8 exactly. |
| `NUMERICAL_STABILITY_TEST` | Kept as-is, reframed as cross-cutting rather than a single scenario (§8). |

No prior-list item was deleted; every one is either kept verbatim or explicitly folded into a master-dataset-aligned name with the mapping stated above.

## Rules

- `gazebo-testing` may create test worlds, test scripts, launch scripts, automated regression tests, and result reports here.
- `gazebo-testing` must never change aircraft physics parameters (mass, CG, inertia, aerodynamic coefficients, control authority, motor thrust) to make a test pass.
- A failing test is reported as `TEST_FAILED` with: observed behavior, expected behavior, evidence/logs, suspected subsystem, and the responsible specialist agent (`geometry-structure`, `aerodynamics`, `propulsion`, or `controls-integration`).
- `validation` reviews test results independently of `gazebo-testing` — the two roles are never merged.

## Current phase

No test scenarios are implemented yet, since no aircraft physics implementation exists in this repository. This file is registration/documentation only — no Gazebo instance was launched and no test was executed to produce it. See `docs/architecture/AGENT_WORKFLOW.md` for the full agent workflow this test infrastructure supports, and `docs/architecture/GAZEBO_READINESS.md` for the current cross-domain data-readiness assessment.
